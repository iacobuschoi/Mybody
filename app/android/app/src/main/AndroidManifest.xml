<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- **릴리스에도 인터넷 권한이 있어야 합니다.**

         flutter create 는 이 권한을 debug/ 와 profile/ 매니페스트에만 넣어
         줍니다. 릴리스 빌드는 main 만 합쳐지므로, 이게 없으면 앱은 깔리고
         켜지지만 서버로 가는 요청이 전부 EACCES 로 막힙니다.

         그런데 그 실패가 화면에는 "서버에 닿지 못했습니다 — 컴퓨터가 꺼져
         있을 수 있습니다" 로 나옵니다. 주인은 켜져 있는 노트북을 보면서
         영문을 모르게 됩니다. 게다가 `flutter run` 이나 웹으로 시험할 때는
         권한이 있어서 **멀쩡히 동작합니다** — 친구 폰에 들어가야 드러납니다. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 간식 단백질 알림 — 서버가 아니라 폰이 직접 예약합니다.
         안드로이드 13부터는 알림을 띄우려면 물어봐야 하고(POST_NOTIFICATIONS),
         재부팅 뒤에도 예약이 살아 있으려면 부팅 신호를 받아야 합니다.
         정확한 시각 알람 권한은 안 씁니다 — 몇 분 늦어도 되는 알림입니다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- 결과지를 **찍어서** 넣을 때 씁니다. 앨범에서 고르는 길은 권한이
         필요 없습니다 (안드로이드가 고른 한 장만 건네줍니다).
         카메라가 없는 기기에서도 앱이 깔려야 하므로 required=false 입니다. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!-- **몸 기록이 구글 드라이브로 넘어가지 않게 합니다.**

         android:allowBackup 을 안 적으면 안드로이드는 **기본을 true 로**
         봅니다. 그러면 자동 백업이 이 앱의 저장소를 통째로 떠서 사용자의
         구글 계정에 올립니다 — 측정 기록 · 프로필 · 목표 전부요.

         이 앱은 "몸 숫자는 이 폰에만 있고 서버로 안 올라갑니다" 를 내세우고
         있고, 개인정보처리방침에도 그렇게 적었습니다. 그런데 정작 안드로이드가
         뒤로 구글에 보내고 있었습니다. 말과 실제가 달랐습니다.

         끄는 대신, 내보내기가 실제로 쓸 수 있는 백업이 되게 고쳤습니다
         (설정 → 내보내기 → 복사하기). -->
    <application
        android:label="Mybody"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 예약된 알림을 시각에 맞춰 띄우는 수신기 (flutter_local_notifications) -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <!-- 설정의 「개인정보처리방침」 링크를 브라우저로 여는 데 씁니다. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
