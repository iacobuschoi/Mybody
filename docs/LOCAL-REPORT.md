# 노트북 보고 — 2026-09-23 00:42 KST (11 끝)

이전 보고(10f041a)를 대체합니다. c906756 을 빌드해 에뮬레이터(pixel_api34)와 웹(localhost:8080)으로 확인. 서버는 `node tools/launch.js` 로 다시 띄웠습니다(pokes 표 · ocr.js 그래프). 코드는 건드리지 않았습니다.

## 11. 운동 독촉 + 그래프 판독 — 전부 됨
- [x] **독촉 보내기** — 됨. emutest1 앱 → 친구 탭 → emutest2 줄(오늘 체크 없음)에 종 버튼 → 토스트 **"emutest2님에게 운동 독촉을 보냈습니다 💪"** → 종이 회색으로. 친구 상세 상단에 「운동 독촉하기」 버튼. 서버 `pokes` 행 생성(from emutest1 → to emutest2, kind workout). "오늘 운동 체크한 친구에겐 버튼 없음" 은 그런 친구가 없어 못 봄.
- [x] **독촉 받기** — 됨. emutest2 로 로그인 → 알림 **「emutest님이 운동하라고 콕 찔렀어요 / 오늘 운동 어때요? 💪」**(알림창에서 확인) + 친구 탭 맨 위 초록 띠(같은 문구 · 2026. 9. 23.). ✕ 로 닫힘. 앱 강제 종료 후 다시 켜도 안 옴(GET /pokes 는 가지만 빈 응답). 서버 `delivered_at` 채워짐. 웹 푸시는 emutest2 웹에 구독이 없어(알림 거부 상태) 못 봄.
- [x] **하루 한 번** — 됨. 상세에서 다시 누르면 서버 400, 목록에서 앱 재시작 후 다시 누르면 토스트 **"오늘은 이미 보냈습니다 — 하루 한 번만"**.
- [x] **그래프 판독(앱)** — 됨. emutest3(빈 계정)에 주인 결과지 사진 → 「사진에서 읽기」 → 안내 끝에 **"아래 그래프의 지난 측정 2개도 읽었습니다 — 다음 화면에서 같이 저장할 수 있습니다."** → 「다음 — 검산하기」 → 맨 아래 **「지난 측정 2개도 같이」** 카드: 2026. 6. 30. 체중 89.0 · 골격근 36.2 · 체지방률 28.4% / 2026. 8. 31. 86.9 · 37.4 · 24.2%, 둘 다 체크 → 「저장하기」 → 토스트 "지난 기록으로 저장했습니다 — 계획은 그대로입니다 · **지난 측정 2개 추가**" → 추이 탭 점 4개(6/30 · 8/31 · 9/19 · 9/22), 「처음부터 지금까지 6/30 → 9/22」, 측정 기록에 6/30 · 8/31 있음. 같은 결과지 다시 판독 → 안내에 그래프 줄 없고 검수 맨 아래 카드 없음. 6/30 · 8/31 을 지운 뒤 다시 판독 → 카드 2개 → 8/31 체크 풀고 저장 → 토스트 "지난 측정 **1개** 추가", 기록에 6/30 만 추가.
- [x] **그래프 판독(웹)** — 됨. emutest2 웹 P03 에 같은 사진 → 자동 판독 켜기(동의 창) → 「서버에 판독 맡기기」 → P04 「측정 정보」 카드 안 체크박스 **「아래 그래프의 지난 측정 2개도 같이 저장」 · "2026. 6. 30. · 2026. 8. 31. — 내 기록에 없는 날만"**(기본 체크) → 「나중에 (저장만)」 → (프로필 키가 달라 "값을 한 번만 확인해 주세요" 창 → 「이대로 저장」) → 토스트 **"측정이 저장되었습니다 · 지난 측정 2개도 추가"** → P10 기록 화면에 8/31 · 6/30 이 **「기기 미상 · 그래프 복원」** 으로, 필터 칩 "전체 4 · 사진판독 3 · 직접입력 1". 저장된 시각 6/30 07:36 · 8/31 08:35 (결과지 인쇄 시각 그대로).
- [x] **서버 [ocr] 오류 없음** — POST /api/ocr 5회 전부 200(6~9초), err 스트림 비어 있음. 그래프 없는 결과지(합성 샘플)는 "읽었습니다 — 맞는지 보고 넘어가 주세요" 만, 카드 없음 — 전과 같음.

## 눈에 띈 것
- **같은 결과지를 두 번 저장하면 본 측정이 중복됩니다.** 그래프 항목은 "내 기록에 없는 날만" 걸러지는데, 본 측정(9/19)은 안 걸러져서 기록에 9/19 가 두 줄 생겼습니다(앱). 웹도 같을 것으로 보임(안 봄).
- 웹 P10 필터가 그래프 복원 항목을 「사진판독」 으로 셈(3 = 판독 1 + 복원 2). 의도라면 그대로.
- 릴리스 v0.2.1 (c906756, apksigner 지문 주인 열쇠) 확인. x86_64 수정(4d20f26) CI 는 도는 중 — 끝나면 APK 안 lib/ 목록으로 확인해 다음 보고에 적겠습니다.

## 그 밖에
- 에뮬레이터에는 디버그 빌드(c906756) 가 emutest3 로 로그인돼 있음. 시험 계정 emutest1 · emutest2 · emutest3 — 지워도 됨. 서버 `pokes` 1행, `records` 4행(시험 계정).
- 우편함 계속 봄.

**11 끝.**
