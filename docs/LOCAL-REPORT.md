# 노트북 보고 — 2026-09-22 17:32 KST (9 끝)

이전 보고(50d8f30)를 대체합니다. c270b3f · 9958d74 를 빌드해 에뮬레이터(pixel_api34)와 웹(localhost:8080, 같은 서버)으로 확인. 서버는 `node tools/launch.js` 로 다시 띄웠습니다(db.js 동의 판 2026-09-22 · 로컬·Tailscale 양쪽 /api/health OK). 코드는 건드리지 않았습니다.

## 9. 기록 동기화 — 전부 됨
- [x] **앱 데이터 삭제 → 로그인 → 기록 복귀** — 됨. emutest1: `pm clear` → 첫 실행(알림 권한 프롬프트 → 계정 화면) → 로그인 → **4초 안에** 홈에 측정 2건(86.0 −0.7 · 38.1 +0.2 …), 목표·계획(감량모드 상 · D-146), 운동 일정(1/3일), 식단(5건 2430 kcal · 9/21 기록 점) 그대로. 온보딩을 안 거치고 바로 홈. 측정 상세에 **사진 없음**(설계대로). 서버 로그: POST /api/auth/signin → GET /api/sync/pull.
- [x] **웹 저장 → records 행 → 앱에서 웹 계정 로그인** — 됨. 웹 emutest2 가 운동 체크 → `records` 에 kind='state' id='main' 행(26 KB, 08:16:42Z). 앱 `pm clear` → emutest2 로그인 → 웹 기록이 따라옴: 70/30/15, 리컴프 계획 D-132(웹에서 만든 것), 운동 2/3(월·화 완료 · 수 예정), 점심 비빔밥 638 kcal, 「주로 먹는 것」 비빔밥.
- [x] **두 기기 충돌 — 마지막에 바꾼 쪽이 이김** — 됨(설계대로). 앱(emutest2)에서 측정 71/30.5 추가 → 서버 레코드 08:28:11Z, scans 2. 웹은 옛 상태(scans 1)로 목요일 헬스 예정 저장 → 서버 레코드 08:28:45Z, scans 1 + 목요일 계획. 앱 재실행 → pull → **앱 측정 사라지고 웹 것으로**(홈 70.0 만, 목요일 예정 표시 2/4일). 관찰: pull 직후 앱이 push 를 한 번 더 함(08:29:23, 428 ms)인데 서버 레코드 시각·내용은 안 바뀜(무해). 다른 관찰: 앱에서 직접 넣은 측정의 시각이 그날 09:00 으로 박혀서, 같은 날 웹에서 17:06 에 넣은 측정보다 **앞** 순서가 됨 — 홈 "최신 인바디"가 71 이 아니라 70(−1.0)으로 보였음. 판독 커밋(962a11e)에서 "날짜만 남기면 9시가 박힌다" 고 한 그 자리.
- [x] **설정** — 됨. 「백업」 카드 없고 「계정」 카드(계정 관리 · 로그아웃), 「지우기」 문구가 "서버에 저장된 내 기록(동기화 사본·주간 요약)" 으로. 맨 아래 작은 글씨 「내 기록 내보내기 · 가져오기」 만(서버 주소 바꾸기 없어짐 — 9958d74). 로그아웃 → 설정 닫히고 계정 화면.
- [x] **온보딩 동의문** — 코드 확인: onboarding.dart 253행 "측정 기록·목표·계획·식단은 이 기기와 **내 계정**에 저장됩니다 — 기기를 바꿔 로그인하면 그대로 따라옵니다. 사진은 기기에만 남습니다." RichishText 로 그려서 별표는 굵게로 나옴. 화면으로는 새 계정을 또 만들어야 해서 안 봄.
- [x] **음식 추가 화면(9958d74)** — 됨. 둥근 검색창("음식 이름 — 닭가슴살, 찌개, 김밥…") → 끼니 세그먼트(아침/점심/저녁/간식) → 분류 칩 한 줄 → 「최근에 먹은 것」 · 「목록」 줄마다 왼쪽 이름·성분, 오른쪽 kcal 큼직 + ＋. 고르면 양 시트에 ☆ 와 "반 · 155kcal / 1인분 · 310kcal / 1.5 · 465kcal / 2인분 · 620kcal". 두 개 고르면 아래 칩 두 개(✕ 로 뺌)와 **「저녁에 2개 저장 · 616kcal · 단백질 12g」** 버튼.

## 그 밖에
- 시험 계정 emutest1 · emutest2 · emutest3, emutest1↔emutest2 친구. 서버 `records` 에 emutest1 · emutest2 state 행. 지워도 됨.
- 에뮬레이터 켜 둠(emutest2 로그인 상태). 우편함 계속 봄.

**9 끝.**
