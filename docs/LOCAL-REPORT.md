# 노트북 보고 — 2026-09-22 16:42 KST (7 끝)

이전 보고(9144568)를 대체합니다. 01776ec → b439d0e → 3cc02d2 를 차례로 빌드해 에뮬레이터(pixel_api34, emutest1)와 웹 앱(localhost:8080, emutest2)으로 확인. 서버는 `node tools/launch.js` 로 다시 띄웠습니다(Tailscale funnel 포함, 로컬·Tailscale 양쪽 `/api/health` OK). 코드는 건드리지 않았습니다.

## 7. 친구 흐름 다시 — 전부 됨
- [x] **앱 운동 체크 → 웹 친구 목록** — 됨. 앱(emutest1)이 헬스 체크 → 웹 emutest2 목록에 "이번 주 계획 3일 · 지킴 1일", 상세에 "이번 주 기록함 · 계획 3일 · 지킴 1일 · 2일이 남았습니다". 서버 DB `snapshots` 에 emutest1 행이 저장 때마다 갱신됨(POST /api/snapshots 07:34:41Z, 07:35:06Z). 두 번째 측정(86.0/38.1/19.6)을 넣자 웹 상세 「최근 변화」에 **골격근 +0.2kg · 체중 −0.7kg** — 체중 변화 스위치가 실제로 나감.
- [x] **공유 스위치 저장** — 됨. 체중 변화 켜고 나갔다 들어와도 켜져 있음. 서버 `shares` 행: emutest1→emutest2 `{weightTrend:true, smmTrend:true, streak:true, schedule:true, …}`.
- [x] **비행기 모드** — 됨. 친구 탭에 마지막 목록이 남고 위에 "서버에 닿지 못했습니다 — 컴퓨터가 꺼져 있을 수 있습니다 — 마지막으로 본 목록입니다. 여기서 바꾼 것은 망이 돌아오면 보냅니다." → 친구 상세 열림 → 골격근 변화 스위치 → 토스트 "지금 서버에 못 닿아서 **나중에 보냅니다.**" → 비행기 모드 끄고 10초 뒤 다시 들어가니 4개 켜짐(기록 · 운동 · 체중 · 골격근), 서버 `shares` 에 smmTrend:true 로 반영(updated_at 07:31:21Z, 망 복구 직후). **단, 토스트 문구에 마크다운 별표 `**` 가 그대로 찍힘** (아래 "눈에 띈 것").
- [x] **웹 운동 체크 → 앱 친구 목록 · 소식** — 됨. 웹 emutest2 가 화요일 헬스 체크 → 앱 목록 "이번 주 1/1일 운동 · 운동 1일째 · 식단 1일째"(첫 관찰, 소식 없음 — 설계대로). 웹에서 월요일까지 체크(2/2일) → 앱 「소식」 배지 1, "emutest2님이 이번 주 2일째 운동했습니다 (계획 2일) · 내가 알게 된 때 · 9/22", 목록 "이번 주 2/2일 운동 · 운동 2일째".
- [x] **앱 친구 상세** — 됨. 스트릭 타일 "운동 1일째 / 식단 1일째", 「오늘 식단」 638 kcal · 탄수화물 85 g · 단백질 31 g · 지방 19 g 막대(웹 emutest2 가 점심에 비빔밥 1인분 적고 「오늘 식단 (탄단지)」 스위치를 켠 뒤), 「이번 주 운동」 7칸에 화 22 점 · "1/1일 완료", 「내가 이 친구에게 보여 주는 것」 접힘("n개 켜짐") → 펼치면 스위치 8개. 목록 줄에 "운동 n일째 · 식단 n일째".
- [x] **아이콘** — Lucide 로 바뀜(탭바 홈/식단/플랜/추이/친구, 인바디 버튼, 스트릭 타일 아령·포크).
- [x] **인바디 올리기 체지방률(3cc02d2)** — 됨. 세 번째 칸 「체지방률」(%). 86.7 / 38 / 23.1 → 검수에 체지방량 **20.0 kg**. 「사진에서 읽기」도 됨 — 실제 결과지 사진 → 체중 86.7 kg · 골격근량 37.9 kg · **체지방률 23.1 %** · 측정일 2026.9.19 로 채움.

## 그 밖에 눈에 띈 것
- **토스트에 마크다운 별표**: 오프라인 공유 토스트가 화면에 `지금 서버에 못 닿아서 **나중에 보냅니다.**` 로 보임(별표 네 개 그대로). 다른 토스트는 안 그럼.
- **친구 상세의 공유 섹션은 오프라인 캐시에 안 들어감**: 비행기 모드에서 상세를 열면 "0개 켜짐"에 스위치가 전부 꺼진 채로 보임(실제로는 3개 켜져 있었음). 거기서 하나 켜면 큐에는 그 하나만 들어가 정상 반영되지만, 사용자는 "다 꺼졌네" 하고 다시 켤 수 있음 → 그러면 서버에 중복 PUT.
- **친구 탭 첫 로딩이 5~9초**: 탭을 누르면 스피너가 5초 이상 돌고, 그 사이 목록 자리를 누르면 아무 일도 안 남(두 번 겪음). 목록은 캐시가 있으니 먼저 보여 주고 갱신하면 될 듯.
- **웹 앱**: 친구 목록이 탭 이동으로는 안 갱신됨(새로 고침 필요) — 이전 보고와 같음.
- 시험 계정 emutest1 · emutest2 · emutest3, emutest1↔emutest2 친구. 서버에서 지워도 됨. emutest1 에 측정 2건(9/19 결과지 · 9/22 수동 86.0/38.1/19.6).
- 에뮬레이터 켜 둠. 우편함 계속 봄.

**7 끝.**
