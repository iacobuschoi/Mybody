# 노트북 보고 — 2026-09-23 18:00 KST (13 끝 · 14 끝)

이전 보고(ff2bf91)를 대체합니다. `git pull`(d2c58ed) → `node tools/launch.js` 로 서버를 다시 띄워
release/ 를 d2c58ed-cde95c 로 새로 만들었습니다. 웹은 localhost:8080, 앱은 CI 빌드 **app-51**
(run 35838260354, 커밋 d2c58ed)을 pixel_api34 에뮬레이터에 `adb install -r`. 코드는 건드리지 않았습니다.

---

## ⚠ 먼저 — 릴리스 AAB 가 **서명이 안 돼 있습니다** (플레이 업로드 막힘)

`gh release download v0.2.3 -p "*.aab"` 로 받은 **mybody-v0.2.3-playstore.aab (83MB)** 는
`keytool -printcert -jarfile` → **"Not a signed jar file"**, `jarsigner -verify` → **"jar is unsigned"**.
zip 안에 루트 `META-INF/MANIFEST.MF`·`*.SF`·`*.RSA` 가 아예 없습니다. 콘솔에 올리면 거부됩니다.

**원인 (CI 로그에서 확인)** — `apk.yml` 의 「만들어졌는지 확인」 단계가

```
aab=$(find app/build -name '*.aab' | head -1)
```

로 찾는데, v0.2.3 을 만든 실행(35810264889)에서는 `find` 가 **서명 전 중간 파일**을 먼저 집었습니다:

```
app/build/app/intermediates/intermediary_bundle/release/packageReleaseBundle/intermediary-bundle.aab
```

같은 실행 로그에 제대로 나온 것은 `build/app/outputs/bundle/release/app-release.aab (32.2MB)` 입니다.
`find | head -1` 순서는 보장이 없어서 **판마다 서명된 것이 나올 수도, 안 나올 수도** 있습니다 —
실제로 오늘 CI 빌드(app-51) 꾸러미의 `bundle/release/app-release.aab` 는 **제대로 서명돼 있습니다**
(SHA256 `06d945a3…83de11`). 즉 서명 자체는 잘 되고, **고르는 데서만** 틀립니다.

**고칠 자리** — `.github/workflows/apk.yml` 213행쯤을 outputs 아래로 한정하면 됩니다:

```
aab=$(find app/build -path '*outputs/bundle/release/*' -name '*.aab' | head -1)
```

(APK 쪽은 `-name 'app-release.apk'` 라 우연히 안 걸렸습니다. v0.2.3 APK 는 지문 확인했고 정상입니다.)

**노트북에서 임시로 한 것** — 주인이 아침에 잘못 올리지 않도록, 바탕화면 폴더의
서명 안 된 파일은 `서명안된것-쓰지마세요/` 로 옮기고 사유를 적은 쪽지를 넣었습니다.
대신 **CI app-51 의 서명된 AAB 를 `mybody-build51-playstore.aab` (32MB, versionCode 51)** 로 넣고
폴더의 README.md 맨 위에 쪽지를 붙였습니다. 다음 릴리스가 고쳐져 나오면 그걸로 바꾸면 됩니다.

---

## 13. 플레이 스토어 준비 — **13 끝**

**폴더 경로: `C:\Users\user\Desktop\Mybody-플레이`**

1. **GitHub Pages** — 건너뛰었습니다(주인이 이미 켬). 확인만: `privacy.html` · `delete-account.html`
   둘 다 **200 OK**. 두 파일 모두 「여기에 연락받을 이메일 주소를 적어 주세요」 자리표시자가
   **아직 그대로**입니다 — README 0번, 주인 몫이라 안 건드렸습니다.
2. **폴더** — `play/` 전부 복사했습니다: README.md · 등록정보.md · 앱-콘텐츠.md · pepk.cmd ·
   make-shots.py · 계정.txt.예시 · 그림/(아이콘 · 피처그래픽 · 스크린샷 9장).
3. **AAB** — 위 ⚠ 참고. 폴더에 들어간 것은 `mybody-build51-playstore.aab`(서명 확인됨).
4. **심사용 계정** — 웹에서 `playreview` 가입(건강정보 동의 체크 · 온보딩 3단계까지 · 복구 코드 받음).
   서버 DB 에 `consent_version=2026-09-22`(새 동의 판)로 들어갔습니다. 로그인도 따로 확인했습니다.
   가입 코드는 **필요 없습니다** — `/api/health` 가 `"openSignup":true`. emutest1 과 **친구까지 맺어 뒀습니다**
   (playreview 가 요청 → emutest1 이 수락 → 양쪽 친구 목록에 서로 보임).
   아이디·비밀번호·복구 코드는 폴더의 **`계정.txt`** 에만 적었습니다(저장소엔 안 넣음, .gitignore 확인).
   ※ emutest1 은 비밀번호를 몰라서 `tools/reset-password.js` 로 새로 정했습니다 — 그 값도 계정.txt 에 있습니다.
5. **스크린샷** — **다시 안 찍었습니다.** 저장소 `play/그림/스크린샷/` 9장을 그대로 복사했습니다(지시대로).
6. **열쇠 확인** — `keytool -list -v`:
   `SHA256: 06:D9:45:A3:83:79:71:CE:A7:AF:0C:03:BC:EF:F3:13:96:4F:57:7B:C8:C1:6A:41:03:3A:A2:60:17:83:DE:11`
   → **맞습니다**(Owner CN=Mybody, O=Mybody, C=KR · 2054-02-07 까지). 열쇠 폴더는 안 건드렸습니다.
   `pepk.cmd` 는 pepk.jar 없이 돌려 보니 **"pepk.jar 가 이 폴더에 없습니다"** 로 멈추고 종료코드 1 — 의도대로입니다.
   (자바는 `C:\Program Files\Java\jdk-21` 에 있고 PATH 에는 없습니다. pepk.cmd 의 `where java` 는
   `Common Files\Oracle\Java\javapath\java` 로 통과하니 더블클릭해도 됩니다.)
7. **README 훑기** — 순서는 맞습니다. 안 맞았던 곳:
   - AAB 파일 이름(위 ⚠) — 폴더 사본만 고쳐 뒀습니다. **저장소 `play/README.md` 는 안 건드렸습니다.**
   - 「이 폴더에 있는 것」 표에 `계정.txt.예시` 와 `make-shots.py` 가 빠져 있습니다(사소).
   - 0번의 Pages 안내가 「Settings → Pages → Branch 기본 브랜치 · `/docs`」인데 주인이
     **Source=GitHub Actions** 로 켰습니다. 안 열릴 때의 대처로는 지금 틀린 안내입니다.

---

## 14. 오늘 바뀐 것 — **14 끝**

### 1. 웹 (localhost:8080 · release/ d2c58ed-cde95c) — 전부 됨
- [x] `김치찌게` → 머리글 **「'김치찌게' 와 비슷한 이름」**, **김치찌개**(배지 "비슷한 이름")가 맨 위. 이어 김치찜 · 배추김치 …
- [x] `ㄷㄱㅅㅅ` → **닭가슴살(생)** 부터 8개, 배지 "초성"
- [x] `ekfrktmatkf` → 같은 목록, 배지 "한/영 자판"
- [x] `아이스아메리카노` → **아메리카노** 한 줄(정확)
- [x] `물냉면` → **냉면(물)** 한 줄
- [x] `zzzz` → **「찾는 음식이 없습니다」** + 「직접 입력」
- [x] **검색어가 안 바뀝니다** — 머리글이 친 글자를 그대로 인용하고 입력칸도 그대로.
- [x] `초밥` → 초밥(모둠/연어/광어/새우) · 초밥 1개 · 유부초밥 … / `프로틴바` → 프로틴바 일반·저당·고단백 20g /
      `단백질바` → 같은 셋(별명으로 잡힘)
- [x] 음식표 **432개** (`release/js/fooddb.js` 의 `f(` 줄 수)

### 2. 에뮬레이터 (app-51, pixel_api34 / API 34) — 전부 됨
- [x] **홈에 「플랜」 카드 없음** — 최신 인바디 → 목표까지 → 이번 주 운동 → 다음에 할 일, 이게 전부입니다.
      최신 인바디에 이전 대비 변화량(−0.7 / +0.2 / −0.4 / −0.3)도 보입니다.
- [x] **식단 탭 위 「오늘/달성률」 토글 없음** — 맨 위는 날짜 줄(목17 … 수23), 그다음 「오늘」 카드,
      「뭘 먹을까 · 단백질 147g 남음」 버튼, 「주로 먹는 것」 칩, 끼니 카드 넷.
- [x] **플랜 탭 위 「플랜 / 달성률」** 있음.
      - 달성률 → **주간/월간** 토글. 주간: 「운동 · 최근 7일」 계획 1일 / 지킨 날 1일(100%) / 놓친 날 0일 ·
        "계획한 날만 세고, 쉬는 날은 끊지 않습니다" · 7칸 점, 「식단 · 최근 7일」 기록 2/7일 · 칼로리 평균 1835(75%) ·
        단백질 평균 82g(56%) · 범위 안 1일 · 단백질 달성 1일 · 단백질 주간 막대.
      - 월간: 「운동 · 최근 30일」 30칸 달력(지킴/놓침/쉬는 날 범례) · 「식단 · 최근 30일」 기록 2/30일.
- [x] **홈 → 이번 주 체크인** — 「얼마나 지켰나」 카드 **없음**. 「지난 7일 실행」에 운동 **1/1일(100%)** ·
      식단 **1/2일**("기록 2일 — 3일은 있어야 봅니다")만. 안내문도 "플랜 탭 「달성률」과 같은 숫자".
- [x] **음식 추가 앱바 오른쪽 「직접 입력」** 있음. 화면 모양도 9번 때 그대로(둥근 검색창 → 끼니 고르기 →
      분류 칩 → 즐겨찾기 ☆ → 최근에 먹은 것 → 목록, 오른쪽 kcal 큼직).
- [x] **앱 검색이 웹과 같음** — `ekfrktmatkf` → 「'ekfrktmatkf' 와 비슷한 이름」 + 닭가슴살(생)…(배지 "한/영 자판"),
      `zzzz` → 「찾는 음식이 없습니다」 + 「직접 입력」. 웹과 글자까지 같습니다.
      ※ **한글 검색어는 에뮬레이터에 직접 못 쳤습니다** — `adb shell input text` 는 ASCII 만 보내고
      에뮬레이터에 한글 IME 가 없습니다. 대신 앱 쪽 같은 동작을 시험으로 확인했습니다:
      `cd app && flutter test` → **127개 전부 통과**, 그 안에 「'김치찌게' 와 비슷한 이름」과
      "검색어는 그대로 둔다"를 직접 보는 시험이 있습니다(screens_smoke_test.dart 513~532행).
      검색 코어는 `node tools/test-fuzzy.js` → **72/72 통과**.
      (`tools/test-food-search.js` 는 이 노트북에 playwright 가 없어서 못 돌렸습니다 — CI 몫.)

### 3. 「뭘 먹을까」 간식 — 됨(한 가지만 다름)
단백질 147g 남은 상태에서 열면 **간식** 탭에:
- 저지방우유 200ml × 1.5 + **단백질 음료(셀렉스)** 1병 250ml — 단백질 30g · 250kcal
- 계란(삶음) 1개 + 닭가슴살 소시지 1개 × 2 — 30g · 252kcal
- **프로틴 쉐이크(우유)** 1스쿱30g+우유200ml — 30g · 244kcal

단백질 음료·프로틴 쉐이크는 나옵니다. **「프로틴바」는 후보 셋 안에 없었습니다**(검색으로는 잘 나옵니다).
사먹기 탭은 갈비탕+공기밥+그릭요거트 / 설렁탕+공기밥+두유 / 족발+우유 로 단백질 49~52g.

---

## 그 밖에 눈에 띈 것
- 로그인만 했는데 측정·목표·플랜·식단이 서버에서 그대로 돌아옵니다(9번 동기화). 홈이 바로 채워졌습니다.
- 서버 DB 사용자 13명 중 시험 계정은 emutest1 · emutest2 · emutest3 · **playreview**.
  playreview 는 심사가 끝날 때까지 두세요. 나머지는 지워도 됩니다.
- 바탕화면 폴더의 `계정.txt` 와 `서명안된것-쓰지마세요/` 는 저장소에 안 들어갑니다(확인함).
- 서버와 에뮬레이터는 켜 둡니다. 심사 기간에는 `node tools/launch.js` 가 계속 떠 있어야 심사자가 로그인됩니다.

**13 끝 · 14 끝.** (스크린샷은 새로 안 찍고 저장소 것을 그대로 썼습니다.)

---

# 덧붙임 — 2026-09-23 19:15 KST (v0.2.4 확인 · 폴더 새로 맞춤)

보고(64c4294) 뒤에 클라우드가 고친 것을 노트북에서 확인했습니다.

- **apk.yml 고쳐졌습니다** — 225행이 `find app/build -path '*outputs/bundle/release/*' -name '*.aab'`.
- **v0.2.4 AAB 는 제대로 서명됐습니다** — `mybody-v0.2.4-playstore.aab` **32,274,749 B**,
  `keytool -printcert -jarfile` → Owner `CN=Mybody, O=Mybody, C=KR`,
  SHA256 **06:D9:45:A3:…:83:DE:11**. v0.2.4 APK 도 apksigner 로 같은 지문 확인.
- **에뮬레이터** — v0.2.4 APK 를 `adb install -r` 로 덮어 깔고 켰습니다.
  primaryCpuAbi=arm64-v8a · versionCode **236** · 로그인 상태·기록 그대로 홈이 열림 ·
  logcat 에 UnsatisfiedLinkError·FATAL 없음.
- **바탕화면 폴더 새로 맞췄습니다** (`C:\Users\user\Desktop\Mybody-플레이`) —
  저장소의 새 README.md · 등록정보.md · 앱-콘텐츠.md · 그림/ 으로 덮고,
  **`mybody-v0.2.4-playstore.aab`** 를 넣었습니다. 어제 임시로 넣었던
  `mybody-build51-playstore.aab` 와 `서명안된것-쓰지마세요/` 는 치웠습니다.
  `계정.txt` 는 그대로입니다(playreview · emutest1).
- 폴더에 **주인이 이미 작업한 흔적**이 있습니다 — `pepk.jar` · `encryption_public_key.pem` ·
  **`mybody-pepk.zip`**(4,406 B). README 2번(Play 앱 서명 등록)을 진행 중인 것으로 보입니다.

## 하나 안 맞는 것 — v0.2.4 인데 앱 안에는 **0.2.3** 으로 찍힙니다

v0.2.4 태그는 **27dba08** 에 걸렸고, 「판 이름을 태그에서」(9dfdd10)는 **그 뒤** 커밋입니다.
그래서 이 빌드의 versionName 은 pubspec 값 그대로 **0.2.3** 입니다(versionCode 236 은 정상).

- 플레이는 versionCode 만 보고, 스토어에 보이는 판 번호는 콘솔에서 따로 적으므로 **업로드는 막히지 않습니다.**
- 다만 앱 설정 맨 아래와 콘솔의 내부 테스트 목록에 "0.2.3" 으로 뜹니다.
  `등록정보.md`·`앱-콘텐츠.md` 에는 판 번호가 안 적혀 있어서 글과 어긋나지는 않습니다.
- 신경 쓰이면 9dfdd10 이후로 태그를 다시 끊으면 맞습니다(주인은 그대로 올려도 됩니다).

---

# 덧붙임 — 2026-09-23 20:10 KST (구글 플레이 콘솔 — **검토 제출까지 끝**)

주인이 콘솔을 직접 누르고 노트북 Claude 가 파일·명령·크롬 조작을 맡아 진행했습니다.
**내부 테스트 트랙에 v0.2.4(versionCode 236)를 올리고 검토 제출까지 마쳤습니다.**

## 지금 상태

| | |
|---|---|
| 앱 | `io.github.iacobuschoi.mybody` · 내부 테스트 트랙 **활성** |
| 올라간 판 | **236 (0.2.4)** · 내부 테스터에게 제공됨 · 9/23 19:55 |
| 검토 | **변경사항 8개 제출 완료** — 구글 검토 중 (첫 출시라 몇 시간~며칠) |
| 앱 서명 키 | 주인 열쇠로 등록됨 — 콘솔 지문 `06:D9:45:A3…83:DE:11` |

## 1. Play 앱 서명 — 구글이 만든 열쇠를 주인 열쇠로 **교체했습니다**

앱을 만들자 구글이 자기 열쇠(`91:BD:05:34:54:5E:EA:11…`)를 먼저 걸어 뒀습니다. 그대로
AAB 를 올렸으면 플레이 판이 친구들 폰의 APK 와 **다른 앱**이 됐을 겁니다.
「키 변경 → Java 키 저장소에서 내보내기 및 업로드」로 `mybody-pepk.zip` 을 올려 교체했고,
교체 뒤 콘솔 지문이 `06:D9:45:A3…83:DE:11` 로 바뀐 것을 확인했습니다.

### ⚠ `play/pepk.cmd` 는 이 노트북에서 **그대로는 안 됩니다** — 고쳐야 합니다

두 군데가 막힙니다.

1. **비밀번호를 물어보는 자리에서 죽습니다.** pepk.jar 가 `System.console()` 을 쓰는데
   자동 실행(파이프)에서는 null 이라 `NullPointerException`. → `--keystore-pass=` ·
   `--key-pass=` 플래그로 넘기면 됩니다(`--help` 에 있습니다).
2. **`RSA/NONE/OAEPWithSHA1AndMGF1Padding` 을 못 찾습니다.** README 는 "자바 21 로
   다시 해 보라"고 적혀 있는데 **자바 문제가 아닙니다.** pepk.jar 안에 번들된
   BouncyCastle 이 **서명이 없어서** JCE 가 거부합니다(직접 부르면 `SecurityException`).
   자바 21.0.6 에서도 똑같이 실패합니다.

**되는 방법** — 서명된 bcprov 를 클래스패스 **맨 앞**에 두고 메인 클래스를 직접 부릅니다.
이 노트북은 그래들 캐시에 이미 있어서 아무것도 새로 받지 않았습니다:

```
java -cp "%USERPROFILE%\.gradle\caches\modules-2\files-2.1\org.bouncycastle\bcprov-jdk18on\1.79\<hash>\bcprov-jdk18on-1.79.jar;%~dp0pepk.jar" ^
  com.google.wireless.android.vending.developer.signing.tools.extern.export.ExportEncryptedPrivateKeyTool ^
  --keystore="%USERPROFILE%\mybody-signing-key\mybody.jks" --alias=mybody ^
  --output="%~dp0mybody-pepk.zip" --include-cert --rsa-aes-encryption ^
  --encryption-key-path="%~dp0encryption_public_key.pem" ^
  --keystore-pass=<KEY-INFO.txt 의 비밀번호> --key-pass=<같은 값>
```

나온 zip 안 `certificate.pem` 의 지문이 `06d945a3…83de11` 인지 확인하면 됩니다.
`pepk.cmd` 를 이렇게 고쳐 주세요(bcprov 경로는 `dir /s /b` 로 찾게).

## 2. AAB — v0.2.4 태그를 **두 번** 달았습니다

- 처음엔 `afb6cb8`(5f920b1 이전)에 달았는데, 릴리스 직후 깃허브 API 가 몇 분간
  **에셋 목록을 빈 배열로** 돌려줬습니다. `gh release download` 도 `no assets to download`.
  서명을 확인할 파일이 없어서 릴리스·태그를 지우고 다시 달았습니다.
  **나중에 보니 파일은 있었습니다 — API 복제 지연이었습니다.** (`gh release upload` 가
  `ReleaseAsset.name already exists` 로 튕기면서 드러남.) 앞으로 릴리스 직후 에셋이
  비어 보이면 **몇 분 기다렸다 다시 보세요.**
- 결과적으로는 잘 됐습니다. 다시 단 태그가 `27dba08`(**5f920b1 포함**)이라
  AAB 고르기 버그가 고쳐진 코드로 빌드됐습니다. 32MB · versionCode 236 ·
  `keytool -printcert -jarfile` 지문 `06:D9:45:A3…83:DE:11` 확인.
- 5f920b1 의 `outputs/bundle/release` 한정 + `META-INF/*.RSA` 검사, **잘 돕니다.**
  콘솔 업로드도 거부 없이 통과했습니다.

## 3. 방침 페이지 이메일 — 채웠습니다 (08aa365)

`docs/privacy.html` · `docs/delete-account.html` 의 「여기에 연락받을 이메일 주소를
적어 주세요」 자리를 주인 지시로 `iacobuschoi@gmail.com` mailto 링크로 바꿔 푸시했습니다.
Pages 두 주소 200 · 반영 확인.

## 4. 콘솔에 넣은 값 — `play/앱-콘텐츠.md` 와 다른 곳

거의 그대로 썼는데 **두 군데가 문서와 다릅니다. 문서를 고쳐 주세요.**

1. **「앱 액세스 권한」이 「로그인 세부정보」로 이름이 바뀌었습니다.** 콘솔이 그렇게 안내합니다.
2. **가입 코드 문구가 맞지 않습니다.** 지금 서버는 `openSignup: true` 라 코드가 필요 없는데
   문서에는 "가입 코드 `계정.txt 의 가입 코드` 를 넣으면 됩니다"로 적혀 있습니다.
   심사자에게는 이렇게 바꿔 냈습니다:
   > …새로 가입해 보시려면 「처음이에요」를 누르면 되고, **가입 코드는 따로 필요 없습니다.**…

그 밖에 문서에 없어서 그 자리에서 정한 것:

- **데이터 보안 → 계정 생성 방법**: 「사용자 이름 및 비밀번호」 하나만. 복구 코드는
  로그인 방법이 아니라 「기타 인증」이 아닙니다.
- **이름(표시 이름)을 「필수」로** 냈습니다. `db.js` 의 `signUp` 이 `displayName || h` 라
  표시 이름 없이 쓰는 길이 없습니다 — 사용자가 안 내겠다고 고를 수 없으니 필수입니다.
- **스크린샷은 8장까지**입니다. `play/그림/스크린샷/` 은 9장이라 `01~08`(홈→결과지→검산→
  목표→계획→식단→추이→친구)만 올렸습니다. `09-friend.png` 는 뺐습니다.
  문서에 "8장까지"를 분명히 적어 주세요.
- 스토어 설정 연락처 이메일 = `iacobuschoi@gmail.com` (스토어에 **공개**됩니다).

## 5. 서버

- `git pull`(afb6cb8) → 옛 서버 끄고 `node tools/launch.js` 로 다시 띄웠습니다.
  `release/` 를 afb6cb8-cde95c 로 새로 만들었고, `https://desktop-il9c3if.tail0a8f8f.ts.net`
  바깥에서 들어와집니다. 심사용 `playreview` 로 **실제 로그인 테스트 통과**.
- **자동 시작이 등록돼 있지 않습니다.** 작업 스케줄러·시작 프로그램·레지스트리 Run
  어디에도 없습니다. **재부팅하면 서버가 안 켜집니다** — 심사 기간에 위험합니다.
  `node tools/autostart.js --write` 를 주인에게 권하거나, 오라클 VM 이전(15번)을
  심사 전에 끝내는 쪽이 낫습니다.

## 6. 남은 일

1. 구글 검토 결과 기다리기 (Gmail)
2. 통과하면 **비공개 테스트** 트랙에 친구들 이메일 → 「내부 테스트에서 버전 추가」 → 참여 링크
3. 정식 출시는 **테스터 12명 · 14일 연속** 뒤 「프로덕션 액세스 신청」
4. 위 1·4 의 문서 고침 (`play/pepk.cmd` · `play/README.md` · `play/앱-콘텐츠.md`)

---

# 노트북 보고 (App Store 출시 세션) — v0.2.5 는 이미 나갔습니다 (3ae06b2 · 빌드 242)

**클라우드 세션도 v0.2.5 를 만들던 중이라고 해서 적습니다. 중복 주의.**

- `3ae06b2` 푸시 → 「아이폰 TestFlight」 run 35854844051 성공 → App Store Connect 에
  **0.2.5 (242)** 처리 중. 정식 심사는 이 빌드로 냅니다.
- 내용(앱스토어 심사 대비, 사용자가 고름): ① 첫 화면 「로그인 없이 쓰기」(state.guest) ·
  ② 첫 「사진에서 읽기」 때 Anthropic(Claude) 전송 동의(state.ocrConsent) · ③ 가입 동의·온보딩
  문구를 실제 동작대로(기록 전체 동기화, 계정 삭제까지 보관, 기본 공개 = 스트릭·일정·식단) ·
  ④ http 안내에서 "안드로이드" 빼기 · ⑤ 아이폰 전용 · ⑥ 앱 이름 「MyBody 체성분 플래너」
  (상표 '인바디' 빼기) · docs/support.html(지원 URL) 새로 만듦.
- 서버 `HEALTH_CONSENT_VERSION` 은 그대로('2026-09-22') — 바꾸면 심사 중인 안드로이드 0.2.4 가입이 깨집니다.
- 웹 앱 가입 동의(prototype modals.js M29)·prototype/privacy.html 은 옛 문구 그대로입니다.

**부탁**: 따로 만든 v0.2.5 변경이 있으면 `3ae06b2` 위로 rebase 하고, 다음 iOS 빌드는 **v0.2.6** 으로.

## 노트북 보고 — App Store Connect 입력 끝 (제출 전), 문서 고침 요청

사용자 지시: **이제 수정은 노트북이 하지 말고 MyBody 에게 요청.** 그래서 아래는 부탁입니다.

App Store Connect 에 저장한 것: 이름 「MyBody 체성분 플래너」 · 부제 · 건강 및 피트니스 · 콘텐츠 권한 없음 ·
연령 등급 13+(한국 12+ — 음식표의 맥주·소주·와인 때문에 「음주 언급: 드문」, 「건강·웰빙 주제: 예」) ·
의료기기 아님 · 무료 · 대한민국만 · 버전 0.2.5 · 빌드 242 · 수동 출시 · 스크린샷 9장(6.9″) ·
지원 URL support.html · 개인정보 선택 사항 URL delete-account.html · 앱 개인정보 6항목(게시 전).
심사 정보(연락처·메모·계정)는 비밀번호가 있어야 저장돼서 사용자가 넣습니다.

**부탁 1** — `play/등록정보.md` 「자세한 설명」 두 곳을 v0.2.5 에 맞게(App Store 설명엔 이미 반영):
- 「■ 친구와 함께」 "기본은 거의 다 꺼져 있습니다." → "체중·체지방 같은 몸 숫자는 기본으로 꺼져 있습니다."
- 「■ 기록은 내 것입니다」 → "측정·목표·계획·식단은 이 폰에 저장되고, 로그인하면 내 계정에도 저장돼 폰을 바꿔도
  그대로 따라옵니다. 로그인 없이도 쓸 수 있습니다. 결과지 사진은 폰에만 남고, 「사진에서 읽기」를 누를 때만 판독을
  위해 AI 판독 서비스(Anthropic Claude)로 전송되며 보관되지 않습니다. 광고와 추적 도구는 없습니다."

**부탁 2** — `appstore/등록정보.md` 에 실제 입력값(연령 등급 답·결과와 이유, 개인정보 선택 사항 URL, 설명은 위 고친 판).

참고 — 웹 앱 가입 동의(modals.js M29)·prototype/privacy.html, 안드로이드 0.2.4(플레이 심사 중)는 옛 문구·로그인 필수.

---

# 16 — 거의 끝 (2026-09-24 21:05 KST). 남은 둘은 주인 로그인 대기

- **서버**: `git pull`(b6ff558) → 옛 서버(9/23 20:04 에 뜬 launch.js) 끄고 `node tools/launch.js` 새로 띄움(21:01, 최소화 창).
  끊긴 시간 20초쯤. 바깥 주소 `/api/health` 200, 새 엔드포인트 `/api/me/consent` 는 401(로그인 필요) — 새 코드 확인.
- **1. `node tools/test-consent.js`** → 19 통과 · 0 실패.
- **2. 에뮬레이터** — CI run 35974109185(b5c9208) 의 `app-68` APK 설치 → versionName 0.2.6 · versionCode 258.
  - 이미 가입한 계정(emutest, 로그인된 채)으로 켜자 **「동의 문구가 바뀌었습니다」** 가 바로 뜸 → 동의 → 홈. ✓
  - 껐다 켜면 다시 안 뜸. ✓
  - 「동의하지 않고 로그인 없이 쓰기」: **못 봄** — emutest 는 이제 새 판으로 동의해서 화면이 다시 안 뜹니다.
    옛 판 계정(예: emutest2)으로 로그인해야 볼 수 있는데 비밀번호 입력은 노트북 Claude 가 못 합니다 → 주인 몫.
- **3. 전부 지우기** — 확인창 문구 "…지우고 로그아웃합니다 … 내 계정에 저장된 기록은 남습니다" → 전부 지우기 →
  **로그인 화면**(「로그인 없이 쓰기」 포함)으로 감. ✓ 같은 계정으로 다시 로그인해서 기록이 돌아오는지는 **주인 로그인 대기**
  (지우기 직전에 넣은 체크인 9/24 · 86.4kg 이 돌아오는지가 확인 거리).
- **4. 「목표·기간 다시 정하기」** — 홈이 「계획대로 가고 있습니다」 라 어긋난 상태가 아님 → 건너뜀.
- **5. 주간 체크인** — 86.4kg 입력 → 「이번 주 제안 · **기준 잡음** · 기준 체중을 잡았습니다」, 칼로리 제안 없음 ✓ →
  저장 → 홈 「**이번 주 체크인 완료 · 86.4kg**」 ✓ → 체크인 화면 아래 「지난 체크인 · 최근 1건 · 9/24 86.4kg · 기준 잡음」 ✓ ·
  추이 탭 체중 그래프: 9/24 에 회색 **점 하나**(체크인 1건이라 선이 없음).

**안 맞는 것(작음)**: 추이 그래프 범례의 「체크인 체중」 이 「체중」 과 **같은 회색 실선 표시**라 구분이 안 됩니다.
설명 글은 "점선은 주간 체크인…" 인데 범례엔 점선 표시가 없습니다. 체크인이 1건이라 점선 자체도 아직 안 보입니다.

---

# 17 끝 (2026-09-24 21:50 KST) — 0.2.7 을 두 스토어에

**구글 플레이**
- Releases v0.2.7 `mybody-v0.2.7-playstore.aab` → `keytool -printcert -jarfile` SHA-256 `06:D9:45:A3 … 83:DE:11` 확인.
- 내부 테스트 → 새 버전 → **262 (0.2.7)** 올림(출시명 「262 (0.2.7)」, 출시 노트 = `play/등록정보.md` 「0.2.7」) → 저장 및 출시 →
  **「내부 테스터에게 제공됨」 21:37.** 경고 없음. (그 전 버전 236 은 이미 「제공됨」 = 0.2.4 검토 통과했었음.)
- 스토어 등록정보 **자세한 설명**을 `play/등록정보.md` 대로 바꿔 저장 → 게시 개요 → **검토를 위해 변경사항 1개 제출**
  (「검토 중인 변경사항 · 전체 설명 변경」, 사전 검사 최대 14분 뒤 자동 전송). 「AI 애셋 선언」 은 비워 둠(선택 항목).
- 참고: AAB 32MB 가 브라우저 도구 업로드 한도(10MB)를 넘어, 8.5MB 네 조각으로 올려 페이지에서 합친 뒤
  SHA-256 이 원본과 같은 것을 확인하고 넘겼습니다.

**애플**
- TestFlight 0.2.7 (262) 「제출 준비 완료」 → 버전 번호 0.2.5 → **0.2.7**, 빌드 242 빼고 **262** → 저장 →
  앱 개인정보 **게시** → 심사에 추가 → **심사를 위해 제출** (21:45, 「1개의 항목 제출됨 · 최대 48시간」).
- 심사 정보: 로그인 `playreview` + 비밀번호(주인이 입력), 메모에 두 번째 계정 `emutest1`(비밀번호 주인이 입력), 연락처 — 저장 확인.

**안 맞는 것 / 알릴 것**
1. **`playreview` 계정에 동기화된 기록이 하나도 없습니다**(서버 `records` 에 state 없음). 심사자는 빈 앱에서
   온보딩부터 시작합니다. 측정 하나 · 목표 · 계획이 들어 있으면 심사자가 추이·플랜 화면을 바로 봅니다 — 넣을지, 어떻게
   넣을지(서버 도구로 시드 / 주인이 에뮬레이터에서 로그인해 입력) 판단 부탁합니다.
2. `playreview` 의 동의 판은 **2026-09-22** 라, 0.2.7 로 로그인하면 **「동의 문구가 바뀌었습니다」 가 한 번 뜹니다**
   (애플·구글 심사자 둘 다). 정상 동작이지만 심사 메모엔 안 적혀 있습니다.
3. 서버: 21:01 에 띄운 것 그대로 켜져 있음(`/api/health` 200). 자동 시작은 아직 등록 안 돼 있습니다.

---

# 19 끝 (2026-09-24 22:26 KST) — playreview 샘플 기록 · 서버 자동 시작

**서버 자동 시작 (주인 요청)** — 작업 스케줄러 「Mybody 서버」: 로그온 30초 뒤 창 없이
`node tools/launch.js`(서버 + `tailscale funnel 8080`)를 띄움. 실행 시간 제한 없음 · 배터리여도 계속 ·
죽으면 1분 간격 3번 다시 · 이미 돌면 새로 안 띄움. 로그 `%USERPROFILE%\mybody.log`.
**`serve.js` 만 걸면 안 됩니다** — 바깥 주소는 launch.js 가 띄우는 foreground funnel 이라(`tailscale funnel status`
는 "No serve config"), serve.js 만으로는 재부팅 뒤 바깥에서 못 들어옵니다. 저장소의 `mybody-autostart.cmd`
(autostart.js 가 만드는 것)가 바로 그 serve.js 만 띄우는 판입니다 — 윈도우 쪽 autostart.js 를 launch.js 로 바꿀지 판단 부탁.
시험: 실제 서버를 이 작업으로 갈아 띄움(30초 끊김) → 바깥 `/api/health` 200, funnel "Available on the internet".

**playreview 샘플 기록** (에뮬레이터 0.2.7 · 262, 주인이 로그인만)
- 21:54 에 다른 기기에서 이미 로그인해 **새 판 동의(2026-09-23)** 와 온보딩(남 · 175cm · 30세)이 돼 있었음 →
  이번 로그인 땐 동의 화면 안 뜸. 온보딩도 그 값 그대로.
- 인바디 2건(숫자 입력, 체지방은 % 칸이라 kg→% 로 환산해 넣고 앱이 kg 로 되돌린 값 확인):
  8/27 **76.0 · 32.4 · 17.2kg(22.6%)**, 9/24 **75.1 · 32.6 · 16.3kg(21.7%)** — 둘 다 「검산을 통과했습니다」, 경고 없음.
- 목표: 골격근 33.6 · 체지방률 17.8% → 체중 73.7(자동) · **체지방 13.1kg** (지방 −3.2 · 근육 +1.0, 리컴프).
  기간 고르기: 「추천」 = **상 · 최단 13주 · 2026-12-24 · 2275kcal · 단백질 153g · 주 4일** → 이 계획으로 시작.
  「주의 1」: "가장 공격적인 계획(a=1.0)은 15주로, 최단(13주)보다 오히려 깁니다 …" (안내 문구, 막는 것 아님).
- 식단: 오늘 점심 **제육덮밥 1인분 790kcal**.
- 화면: 홈(D−90 · 16.3→13.1kg · 32.6→33.6kg), 플랜, 추이(세 선이 8/27→9/24 둘째 점까지), 식단 채워짐.
- 로그아웃 → 로그인 화면. 서버 `records`: state 1건 — scans 2 · goal · plan · foodLogs 1 확인.
- 친구: `playreview` ↔ `emutest1` **이미 친구**(accepted, 9/23).

**전원 · 업데이트** (시스템 설정이라 노트북 Claude 는 못 바꿈 — 주인 몫)
- 읽어 본 값: 화면 끄기 · 절전 = 안 함(전원/배터리 둘 다), 최대 절전 = 전원 안 함 · 배터리 1시간.
  덮개 동작은 이 전원 계획에 항목이 없어 못 읽음(기본값이면 절전). **Windows 업데이트 일시 중지: 안 걸려 있음.**
- 주인에게 덮개 「아무 것도 안 함」 · 업데이트 「1주 일시 중지」 · 충전기 연결을 부탁함.

---

# 노트북 보고 — 22:29 바깥에서 서버가 절반쯤 안 닿던 것, 고침 (22:37)

- 증상: 주인 폰(SKT 5G)에서 `ERR_CONNECTION_CLOSED` · 앱 「서버에 닿지 못했습니다」. 노트북에서 부르면 멀쩡했음 —
  **노트북에서 `*.ts.net` 을 부르면 Tailscale 내부망(100.73.56.0)으로 가서 funnel 을 안 거칩니다.** 바깥 확인은
  check-host.net(여러 나라) 같은 외부에서 해야 합니다.
- 원인: funnel 공개 중계 두 대 중 **103.84.155.153 은 OK, 103.84.155.217 은 매번 "Broken pipe"** → 클라이언트가
  어느 쪽을 고르느냐에 따라 절반쯤 실패. 서버·funnel 설정·권한·Tailscale 상태 페이지는 모두 정상.
  `tailscale netcheck` 에 "gateway and self IP changed" — 노트북 쪽 네트워크 매핑이 바뀐 뒤 한 중계만 옛 길을 쥐고 있던 것으로 보임.
- 한 것: funnel/serve reset 후 작업 재시작(효과 없음) → **`tailscale debug rebind` + `tailscale debug restun`** →
  check-host 24곳 전부 200(.217 경유 4건 포함).
- 제안(판단은 MyBody): 같은 일이 네트워크가 바뀔 때마다 다시 날 수 있습니다. 바깥에서 health 를 보는 감시(예: check-host
  API 또는 폰 쪽) + 실패 시 `tailscale debug rebind` 를 거는 작은 감시 작업을 둘지.

---

# 20 끝 (2026-09-24 23:31 KST) — 0.2.8 (N = 269) 을 두 스토어에

**1. 서버** — `git pull`(bf02fab) → 작업 「Mybody 서버」 끝냈다 다시 실행(23:21) → `node tools/test-appversion.js` **66 통과** →
바깥(check-host 10곳, 중계 .153 · .217 둘 다 200 · WebFetch)에서 `/api/version` JSON 확인(latest 셋 · min · urls).

**2. 구글 플레이** — v0.2.8 AAB `keytool` SHA-256 `06:D9:45:A3 … 83:DE:11` 확인 → 내부 테스트 새 버전 **269 (0.2.8)**,
출시 노트 `play/등록정보.md` 「0.2.8」 → 저장 및 출시 → **「내부 테스터에게 제공됨」 23:25**. 경고 없음.

**3. 애플** — TestFlight 0.2.8 (269) 「완료」(23:12 업로드). 0.2.7 은 **「심사 대기 중」** 이었음 →
앱 심사 → 제출 상세 → **제출 취소**(23:27, 상태 「개발자가 취소함」) → 버전 0.2.7 → **0.2.8**, 빌드 262 빼고 **269** → 저장 →
심사에 추가 → **심사를 위해 제출 23:29** (「1개의 항목 제출됨 · 최대 48시간」). 심사 정보(계정 · 비밀번호 · 메모)는 그대로 남아 있음.

**4. app-version** — 전: 셋 다 없음. `node tools/app-version.js --apk=0.2.8 --play=0.2.8` →
**play 0.2.8 · apk 0.2.8 · appstore ""(App Store 출시 뒤에) · min ""** — 바깥 `/api/version` 에서도 같은 값 확인.

참고: 「앱 심사」 목록에 TestFlight 베타 **0.2.4 (237) 가 어제 19:49 부터 아직 「심사 대기 중」** 입니다(외부 테스트 friends 그룹용).

---

# 노트북 보고 — TestFlight friends(외부) 그룹을 0.2.8 로 (2026-09-24 23:40 KST, 주인 요청)

- friends 그룹에 **0.2.8 (269)** 추가 → **베타 앱 심사 제출 23:36** (「심사 대기 중」). 심사용 로그인은 테스트 정보의 `playreview` 그대로.
  「테스트 내용」(테스터에게 보이는 글): 로그인 없이 쓰기 · 인바디 → 목표 → 계획 · 새 버전 안내 · 끼니 알림 · 주간 체크인 ·
  추이 점선 · 피드백 방법. 자동 공지 켬.
- 같은 그룹에 9/23 부터 베타 심사 대기 중이던 **0.2.4 (237) 는 그룹에서 뺌**(빌드 자체는 남음) — 옛 판이 먼저 승인돼
  친구들에게 가는 것을 막으려고. 이제 friends 에는 0.2.8 (269) 하나.
- 공개 링크 https://testflight.apple.com/join/ekNds9xJ 는 0.2.8 베타 승인 뒤 열림.
- 지금 기다리는 것: App Store 0.2.8 (269) 정식 심사(23:29 제출) · TestFlight friends 0.2.8 (269) 베타 심사(23:36 제출).

---

# 21 끝 (2026-09-25 02:08 KST) — 0.2.9 를 TestFlight 내부 · 플레이 내부 테스트에만

- **N**: 실제 빌드 번호는 **274** (플레이 AAB versionCode 274 · 아이폰 빌드 274). `git rev-list --count v0.2.9` 는 태그가
  3bc8a51 에 걸려 276 이 나오지만, 빌드는 f084b1d(274) 에서 만들어졌습니다. 지시의 274 와 같음.
- **1. 서버**: 손대지 않음. `git pull` 만 해 둠(3bc8a51).
- **2. 플레이**: Releases v0.2.9 AAB(32.6MB) SHA-256 `06:D9:45:A3 … 83:DE:11` 확인 → 내부 테스트 새 버전 **274 (0.2.9)**,
  출시 노트 `play/등록정보.md` 「0.2.9」 → 저장 및 출시 → **「내부 테스터에게 제공됨」 02:06**. 비공개 · 프로덕션 안 건드림.
- **3. 애플**: TestFlight 0.2.9 (274) 「완료」(01:58 업로드) · **내부 그룹 `my` 에 자동으로 들어감**(초대 1). friends 에는 안 넣음.
  App Store 탭 안 건드림 — 0.2.8 (269) 「심사 대기 중」 그대로.
- **4. app-version**: `--play=0.2.9` → play **0.2.9** · apk 0.2.8 · appstore "" · min "". 노트북 서버 8080 에서 확인.
- 로그인 화면은 안 나옴(둘 다 로그인 유지 중).

---

# 22 끝 (2026-09-25 02:58 KST) — 0.2.10 (N = 279) · 서버 재시작 · TestFlight 채널

- **1. 서버**: `git pull`(43560a1) → 작업 「Mybody 서버」 끝냈다 다시 실행 — **02:38:16 재시작**(20초쯤 끊김) →
  `node tools/test-appversion.js` **66 통과 · 0 실패**(지시엔 67 이라 적혀 있으나 파일의 시험 수가 66) →
  바깥(WebFetch)에서 `/api/version` 에 `latest.testflight` 칸과 `urls.testflight`(beta.itunes.apple.com/v1/app/6815144446) 확인.
- **2. 플레이**: v0.2.10 AAB(32.6MB) SHA-256 `06:D9:45:A3 … 83:DE:11` 확인 → 내부 테스트 새 버전 **279 (0.2.10)**,
  출시 노트 「0.2.10」 → 저장 및 출시 → **「내부 테스터에게 제공됨」 02:42**. 비공개 · 프로덕션 안 건드림.
- **3. 애플**: TestFlight 0.2.10 (279) 02:31 업로드 → 「완료」(02:5x) → **내부 그룹 `my` 에 자동 배포**(초대 1). friends · App Store 탭 안 건드림
  (0.2.8 (269) 「심사 대기 중」 그대로).
- **4. app-version**: `--play=0.2.10 --testflight=0.2.10` → **play 0.2.10 · testflight 0.2.10 · apk 0.2.8 · appstore "" · min ""**.
- 로그인 화면 없었음.
