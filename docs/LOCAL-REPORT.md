# 노트북 보고 — 2026-09-23 01:07 KST (12 끝)

이전 보고(3e63e47)를 대체합니다. f12fca5 를 빌드해 에뮬레이터(pixel_api34, emutest2)와 웹(localhost:8080, emutest2)으로 확인. 서버는 `node tools/launch.js` 로 다시 띄워 release/ 를 f12fca5 로 다시 만들었습니다. 코드는 건드리지 않았습니다.

## 12. 같은 결과지 두 번 저장 — 전부 됨
- [x] **앱, 같은 결과지 재판독** — 검수 화면 맨 위 초록 **「이미 있는 기록과 같습니다.」** "같은 결과지입니다 — 저장하면 두 줄이 되지 않고 그 기록을 갱신합니다." → 저장 → 토스트 **"이미 있던 기록을 갱신했습니다"** → 측정 기록에 9/19 **한 줄**(86.7).
- [x] **앱, 같은 사진에 체중 88.0** — 노란 **「같은 시각의 측정이 이미 있습니다.」** "2026. 9. 19. · 체중 86.7kg. 값이 다릅니다 — …" + **「이미 있는 기록을 덮어쓰기」** 스위치(기본 꺼짐). 안 켜고 저장 → 토스트 "지난 기록으로 저장했습니다 — 계획은 그대로입니다", 기록에 9/19 **두 줄**(88.0 · 86.7). 88.0 줄을 지우고 다시 판독 → 88.0 → 스위치 **켜고** 저장 → "이미 있던 기록을 갱신했습니다", 9/19 **한 줄(88.0 · 근 37.9 · 지 20.3)**.
- [x] **웹, 같은 사진 재판독** — P04 에 별도 안내는 없고("값을 한 번만 확인해 주세요" 창은 프로필 키 차이로 전과 같이 뜸) → 「이대로 저장」 → 토스트 **"이미 있던 기록을 갱신했습니다"** → localStorage scans 4건 그대로(9/19 ocr 한 건), P10 "총 4회" · 9/19 한 줄.
- [x] **v0.2.2 릴리스 APK** — mybody-v0.2.2.apk 37,578,086 B · versionCode 8 · versionName 0.2.2 · lib/ arm64-v8a 4 · armeabi-v7a 4(x86_64 없음) · 서명 지문 06d945a3…83de11(주인 열쇠). 에뮬레이터에 **그냥 `adb install`** → primaryCpuAbi=arm64-v8a → 켜짐(알림 권한 → 계정 화면), UnsatisfiedLinkError 없음.

## 그 밖에
- 에뮬레이터에는 v0.2.2 릴리스 APK 가 깔려 있음(로그인 전). 시험 계정 emutest1 · emutest2 · emutest3, 서버 `pokes` 1행 · `records` 4행 — 지워도 됨.
- 서버·에뮬레이터는 켜 둠. 우편함은 이제 안 봐도 된다고 하셨으니, 이 보고 뒤로는 새 지시가 와도 자동으로는 안 잡습니다(다시 시작하려면 노트북 세션에 말씀).

**12 끝.**
