# 노트북 보고 — 2026-09-22 16:12 KST (5 · 6 끝)

이전 보고(fd12285)를 대체합니다. 962a11e → c6de46e → f66d923 → 40eae20 을 차례로 빌드해 에뮬레이터(pixel_api34)에서 확인. `flutter test` 82개 통과(962a11e). 코드는 건드리지 않았습니다.

## 5. 재확인 — 전부 됨
- [x] **흰 글자** — 됨. 친구 화면 구역 제목 "친구", 로그인 전 제목, 강도 카드 제목 "상 · 최단 / 중 · 표준 / 하 · 여유", 홈 카드 제목("최신 인바디" · "목표까지" · "이번 주 운동") 모두 보임.
- [x] **사진** — 됨. 앨범 사진 붙이고 판독 → 검산 → 저장 → 측정 상세 맨 위에 사진이 보이고, 누르면 결과지 전체가 확대 뷰어로 뜸.
- [x] **판독 나머지 칸** — 됨. 검수 「나머지 칸」에 체지방률 23.1 · BMI 24.8 · 제지방량 66.7 · 내장지방 8 · 기초대사량 1810 · 복부지방률 0.94 · InBody 점수 73 · 체수분 48.7 · 단백질 13.3 · 무기질 4.70 — 전부 결과지 인쇄값(계산값 아님). 측정일 2026.9.19. 시각은 화면 어디에도 안 나와서 못 봤음(상세 제목이 날짜만).
- [x] **뒤로 가기** — 됨. 식단 탭에서 뒤로 → 홈 탭. 홈에서 뒤로 → 앱 닫힘.
- [x] **목표 방향 칩** — 됨. "목표 방향 · 리컴프 (지방↓ + 근육↑ 동시)".
- [x] **목표 화면(c6de46e)** — 됨. 셋째 칸 「목표 체지방률 (자동)」(%). 골격근 40 → 체지방률 12.6 으로 자동, "세 숫자가 서로 안 맞습니다" 안 뜸. 체지방률 15 직접 입력 → 체중이 (자동) 82.8. 모드 카드는 제목 + 한 줄 + 「자세히」(펼치면 긴 설명, 「접기」). 모드 고르기 목록에 이름 다 보임(감량모드 "앱의 선택" · 커팅모드 · 리컴프 · 근성장모드 · 유지모드…).
- [x] **홈(f66d923)** — 됨. 「플랜대로 채우기 (상하체 4분할 · 남은 3일)」 → 화·목·금에 표시, "0/3일 완료". 「플랜」 카드(오늘/이번주/한달 · 화요일 하체 A · 2445 kcal · 147 g · 전체 플랜 보기). 「다음에 할 일」(이번 주 체크인 · 측정 기록 보기). 「헬스 했어요」 → "오늘 할 것 다 했습니다 — 헬스" · 1/3일 완료 · 운동 1일 연속. 변화량(±)은 측정이 1개라 못 봄.
- [x] **기간 고르기(40eae20)** — 됨. 「이 목표는」 카드에 첫 문장 + "주의 2" 표시 + 「자세히」 → 구간 설명 · 주의 2개 · 병목이 펼쳐지고 「접기」.

## 6. 친구 흐름 — 에뮬레이터 = emutest1, 웹 앱(localhost:8080, 같은 서버) = emutest2
- [x] **초대 코드 → 친구** — 됨. emutest1 의 코드를 웹 「친구 추가」에 넣음 → 웹 "보낸 요청 1건 · 수락을 기다리는 중" → 에뮬레이터 「받은 요청 emutest2 」 수락 → 양쪽 목록에 서로 뜸(웹 "emutest님과 친구가 되었습니다").
- [ ] **공유 스위치 → 상대 쪽 반영** — **안 됨.** 두 가지 원인:
  1. **Flutter 앱은 주간 요약을 서버에 한 번도 안 올립니다.** `api.dart` 의 `publishSnapshot`(243행)과 `sync_queue.dart` 의 'snapshot' 처리는 있는데, 그걸 부르는 곳이 앱 어디에도 없음(grep: api.dart · sync_queue.dart 안에서만 등장). 웹은 `store.js` 의 save() 가 publishWeekly() 를 불러 저장 때마다 올림(171~177행, 638~680행 — 주석 "이게 없어서 친구 기능의 절반이 존재하지 않았다"). 그래서 웹의 emutest2 화면은 운동 체크·공유 켜기 뒤에도 계속 "emutest — 아직 올라온 기록이 없습니다".
  2. **공유 스위치가 저장되지 않습니다.** 친구 상세에서 체중 스위치를 켜면 켜진 걸로 보이는데, 나갔다 다시 들어오면 꺼져 있음(두 번 재현). 서버로 안 가거나, 다시 읽을 때 서버 값으로 덮이는 것.
- [ ] **비행기 모드 → 공유 스위치 → "나중에 보냅니다"** — **못 함.** 비행기 모드에서 친구 탭이 "서버에 닿지 못했습니다 — 컴퓨터가 꺼져 있을 수 있습니다" 를 띄우고 **친구 목록이 통째로 빔**(목록을 캐시하지 않고 매번 서버에서 가져옴) → 친구 상세에 못 들어가서 스위치를 못 누름. 비행기 모드를 끄고 8초 뒤에도 자동 복구 안 됨, 탭을 옮겼다 오면 복구.
- [ ] **소식** — 안 됨. 웹(emutest2)에서 오늘 헬스 체크(웹 화면 "22일 화요일, 헬스 완료 · 1/1일 완료") → 2분 뒤 에뮬레이터 친구 목록 "emutest2 — 이번 주 공유한 것이 없습니다", 「소식」 "아직 소식이 없습니다". 반대 방향(에뮬레이터 → 웹)은 위 1번 때문에 애초에 안 감.

## 그 밖에 눈에 띈 것
- 웹 앱(prototype) 친구 목록은 탭을 옮겨서는 안 새로 고쳐지고 페이지를 다시 열어야 갱신됨(수락된 뒤에도 "수락을 기다리는 중"으로 남음).
- 검수 화면 판독값 표기 38.0 → "38", 20.0 → "20" 그대로(값은 맞음).
- 시험 계정 emutest1 · emutest2 · emutest3 (emutest1 ↔ emutest2 친구). 서버에서 지워도 됨.
- 에뮬레이터는 켜 둠. 우편함 계속 봄.

**5·6 끝.**
