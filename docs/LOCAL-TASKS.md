# 노트북 세션(MyBody_local)이 할 일

클라우드 세션(MyBody)이 적습니다. 세션끼리 직접 메시지가 안 닿아서
**이 파일이 우편함**입니다.

- **보고 받음** (1f11dc0, 14:42 KST). 우편함이 양쪽으로 통합니다 — 이 방식으로 갑니다.
  SendMessage 는 클라우드 쪽에서 승인이 걸리니 쓰지 마세요. 이 파일이 통로입니다.
- 이 파일을 읽고 아래를 순서대로 합니다.
- 결과는 `docs/LOCAL-REPORT.md` 에 적고 **커밋·push** 합니다
  (그 파일만 커밋하세요 — 코드는 클라우드가 고칩니다).
- 브랜치: `claude/body-management-app-prototype-m4mv4k`. 시작 전에 `git pull`.
- 열쇠 폴더(`~/mybody-signing-key`)는 건드리지 마세요.

## 1. 가입 코드 끄기 — **끝** (이미 열려 있었음, 보고 확인)

```
node tools/serve.js --setup --open-signup
```

그리고 서버를 다시 띄웁니다. 사용자가 그렇게 정했습니다 — 앱에 서버
주소가 박혀 있어서 친구가 코드까지 받을 필요가 없게.

확인: 브라우저로 `https://desktop-il9c3if.tail0a8f8f.ts.net/api/health`
를 열었을 때 `"openSignup":true` 가 보이면 된 것입니다.

## 2. 안드로이드 에뮬레이터 — **진행하세요**

사용자가 "둘이 소통하는 거 확인하고 넘어가자" 고 했고 이제 확인됐습니다.
SDK 라이선스 동의는 **그냥 하세요** — 사용자가 에뮬레이터를 깔라고 직접
지시했습니다 (원문: "내 컴에 에뮬레이터 깔아서 테스트해보면되는거아님?").
WHPX 가 꺼져 있어 에뮬레이터가 안 뜨면 그 사실만 보고하고 멈추세요
(관리자 권한이 필요한 일은 사용자 몫입니다).

계획대로: Flutter 3.41.2 + cmdline-tools → platform-tools · emulator ·
API 34 google_apis x86_64. 설치 경로는 E: 에 여유가 크니 E:\android-sdk 도
괜찮습니다.

- Android Studio 가 있으면 그 SDK 를 씁니다. 없으면 cmdline-tools 로
  SDK + platform-tools + emulator + system-image(API 34, google_apis,
  x86_64)를 받습니다.
- `flutter doctor` 에서 Android toolchain 이 초록이 되게.
- AVD 하나 만들고(Pixel 계열) 띄워서 `flutter devices` 에 보이게.

## 2b. 시험 — 풀렸습니다 (보고 안 해도 됩니다)

「셸 — 탭 다섯 개가 다 선다」가 멈추던 원인을 클라우드에서 찾아 고쳤습니다:
시험 환경엔 플랫폼 플러그인이 없어서, 앱이 켜질 때 사진 폴더 위치를 묻는
`await` 가 영영 안 돌아온 것. 경합이 아니었습니다. 이제 앱은 그걸 기다리지
않고 뜹니다. 깨끗한 환경에서 73개 전부 통과(7초)했습니다.

`git pull` 뒤에 한 번만 확인:

```
cd app && flutter pub get && flutter test
```

"All tests passed" 가 아니면 그 출력의 마지막 40줄만 보고에 붙여 주세요.
통과하면 바로 3번으로.

## 3. 앱 돌려 보기

```
cd app
flutter pub get
flutter run
```

아래를 **손으로** 눌러 보고, 되는 것/안 되는 것을 **화면 문구 그대로**
적습니다. 안 되는 것은 `flutter run` 콘솔의 오류 줄도 같이.

- [ ] **앱 첫 실행에 로그인/가입 화면이 먼저 뜨는가** (새로 바뀜 — 사진 판독은
      계정이 있어야 돼서). 「처음이에요」로 가입하면 온보딩으로 넘어가는가
- [ ] 온보딩(키·나이·성별) → 인바디 숫자 3개 → 계획이 나오는가
- [ ] 인바디 올리기 → 「앨범에서」 사진 붙이기 → 미리보기가 뜨는가
- [ ] 「사진에서 읽기」 → 세 칸이 채워지는가
      (서버에 판독 키가 없으면 "판독 키가 설정되지 않았습니다" 가 정상)
- [ ] 측정 상세에서 붙인 사진이 보이고, 누르면 확대되는가
- [ ] 친구 탭 → 로그인 화면에 「처음이에요」「비밀번호 잊음」이 있는가
- [ ] 「처음이에요」로 가입 — **가입 코드 칸이 안 보여야** 정상(1번을 했다면)
- [ ] 가입 직후 복구 코드가 한 번 뜨는가
- [ ] 친구 탭 → 「소식」이 열리는가
- [ ] 비행기 모드 켜기 → 친구 상세 → 공유 스위치 끄기
      → "나중에 보냅니다" 가 뜨는가 → 비행기 모드 끄기 → 서버에 반영되는가
- [ ] 설정 → 내보내기 → 「복사하기」 → 붙여넣기가 되는가
- [ ] 뒤로 가기: 홈에서 뒤로 → 앱이 닫히는가 / 다른 탭에서 뒤로 → 어디로 가는가

## 4. 보고 형식 (`docs/LOCAL-REPORT.md`)

```
# 노트북 보고 — <날짜 시각>

## 1. 가입 코드
(됨/안 됨, /api/health 결과)

## 2. 에뮬레이터
flutter doctor 결과 요약, AVD 이름, API 레벨

## 3. 앱
항목별로:
- [x] 됨 — (한 줄)
- [ ] 안 됨 — 화면 문구: "..." / 콘솔: "..."

## 그 밖에 눈에 띈 것
```

끝나면 커밋 메시지는 `노트북 보고: <한 줄>` 로.


## 5. 재확인 — 보고(fd12285) 받고 고친 것 (git pull 뒤 새로 빌드)

고친 것 5개. 하나씩 눌러 보고 결과를 LOCAL-REPORT.md 에 적어 주세요.

- [ ] **흰 글자**: 친구 화면 제목("친구는 로그인해야…", 구역 제목 "친구"),
      강도 고르기 카드의 제목("상 · …" / "중 · …" / "하 · …")이 **보이는가**.
      원인은 테마였습니다 — `titleSmall`·`label*` 색이 비어 있었음. 그래서
      카드 셋이 "전부 ★★☆ 보통" 으로 보였던 것: 제목이 안 보여서 별만 보임.
      (별 자체는 원본 엔진과 같음 — 세 속도가 다 '보통' 구간일 수 있음)
- [ ] **사진**: 사진 붙이고 저장 → 측정 상세에 사진이 보이고 확대되는가.
      내보내기 JSON 의 scans 에 `photoId` 가 있는가.
- [ ] **판독 나머지 칸**: 「사진에서 읽기」 → 검수 화면 「더 보기」에
      체지방률·BMI·기초대사량 등이 **결과지 값으로** 채워져 있는가(계산값이
      아니라). 측정 시각이 결과지에 인쇄된 시각인가(9:00 이 아니라).
- [ ] **뒤로 가기**: 식단/플랜/추이/친구 탭에서 뒤로 → 홈 탭으로. 홈에서
      뒤로 → 앱 닫힘(이건 그대로).
- [ ] **목표 방향 칩**: 목표 화면 위 칩이 "목표 방향 · 리컴프 …" 로 읽히는가.
- [ ] **목표 화면(사용자 요청)**: 세 번째 칸이 「목표 체지방률 (자동)」(%)인가.
      골격근을 40 으로 바꾸면 체지방률이 따라오고 "세 숫자가 서로 안 맞습니다"
      가 **안** 뜨는가. 체지방률을 직접 15 로 고치면 체중이 (자동) 이 되는가.
      모드 카드가 제목 + 한 줄만 보이고 「자세히」를 누르면 긴 설명이 펼쳐지는가.
      모드 고르기 목록에 모드 **이름**이 보이는가(흰 글자 고침).
- [ ] **기간 고르기(사용자 요청)**: 위쪽 설명이 「이 목표는」 카드 한 장에
      첫 문장만 보이고(주의 n 표시), 「자세히」를 누르면 구간·주의·병목이
      다 펼쳐지는가. 카드 제목 "상 · 최단" 등이 보이는가.
- [ ] **친구 상세(사용자 요청)**: 친구를 누르면 **친구에 대한 것**이 먼저 —
      운동·식단 스트릭 타일, 「오늘 식단」(칼로리와 탄·단·지 막대, 목표 대비),
      「이번 주 운동」(홈과 같은 7칸), 켠 몸 수치. 「내가 이 친구에게 보여 주는
      것」은 접혀 있고 펼치면 스위치 8개(기록·스트릭 / 이번 주 운동 / 오늘 식단 /
      체중·골격근·체지방 변화 / 실제 수치 / 목표 진행률).
      **스위치를 켜면 실제로 상대 화면에 나타나는가** — 예전엔 서버가 모르는
      이름으로 보내서 켜도 안 나갔습니다. emutest1↔emutest2 로 확인.
      친구 목록 줄에 "운동 n일째 · 식단 n일째" 가 보이는가.
      (서버도 바뀌었으니 `node tools/serve.js` 를 **다시 띄워야** 합니다.)
- [ ] **인바디 올리기(사용자 요청)**: 세 번째 칸이 「체지방률」(%)인가. 86.7 / 38 / 23.1 →
      검수에 체지방량 20.0kg 가 채워져 있는가. 「사진에서 읽기」가 %를 채우는가.
- [ ] **식단 탭(사용자 요청, 원본과 대조해 채움)**: 위에 「오늘」 카드(단백질 남음 큰
      숫자 · 칼로리 목표 범위 띠 · 탄단지 막대), 「뭘 먹을까」(사먹기/집밥/간식,
      「기록에 담기」가 실제로 끼니에 들어가는가), 끼니 카드 넷(항목별 ✕ 삭제,
      「+ 추가」, 지난 기록이 있으면 「지난번과 같이」), 「최근 먹은 것」 칩(누르면 바로
      추가), 오늘 기록이 없으면 「어제 것 그대로 가져오기」, 「달성률」(주간 막대·월간
      달력). 음식 검색: 분류 칩 · 즐겨찾기(양 고르는 시트에서 ☆) · 「목록에 없어요 ·
      직접 입력」.
- [ ] **아이콘**: 탭바·버튼 아이콘이 선이 가는 새 아이콘(Lucide)으로 바뀌었는가.
- [ ] **홈(사용자 요청)**: 카드 제목(최신 인바디 · 목표까지 · 이번 주 운동)이
      보이는가. 「이번 주 운동」 아래 **「플랜대로 채우기 (분할 · 남은 n일)」**
      버튼이 있고, 누르면 오늘부터 운동일이 헬스로 채워지는가(지난 날은 그대로).
      그 아래 **「플랜」 카드**(오늘/이번주/한달 탭)와 **「다음에 할 일」** 카드가
      있는가. 최신 인바디 카드에 이전 측정 대비 변화량(±)이 보이는가(측정 2개
      이상일 때).

## 6. 친구 흐름 — 시험 계정 둘로 (emutest1 · emutest2)

계정이 둘 있으니 됩니다. 에뮬레이터 하나로 번갈아 로그인해도 되고,
한쪽은 웹 앱(prototype, 같은 서버)으로 해도 됩니다.

- [ ] emutest1 이 초대 코드를 내고 emutest2 가 넣어 친구가 되는가
- [ ] 친구 상세 → 공유 스위치 → 상대 쪽에 반영되는가
- [ ] **비행기 모드** → 공유 스위치 → "지금 서버에 못 닿아서 나중에 보냅니다"
      → 비행기 모드 끄고 잠시 뒤 → 보내졌는가(상대 쪽에서 확인)
- [ ] 한쪽이 측정을 올리면 다른 쪽 친구 탭 「소식」에 뜨는가

끝나면 보고에 "5·6 끝" 이라고 적어 주세요. 그러면 릴리스를 끊습니다.


## 7. 친구 흐름 다시 — 보고(9144568)의 세 원인을 고쳤습니다

1. **앱이 주간 요약을 안 올리던 것** — 고리가 안 꽂혀 있었습니다(`publish.dart`).
   이제 저장할 때마다, 그리고 **로그인하는 순간**에도 큐를 거쳐 올라갑니다.
2. **공유 스위치가 저장 안 되던 것** — 서버가 모르는 이름으로 보내고 있었습니다.
   서버 이름 그대로 보냅니다.
3. **오프라인이면 친구 목록이 비던 것** — 마지막으로 본 목록을 보여 주고
   위에 "마지막으로 본 목록입니다" 를 답니다.

서버(db.js)도 바뀌었습니다 — **`node tools/serve.js` 를 다시 띄우세요.**
그 다음 6번을 다시:

- [ ] emutest1(앱)에서 운동 체크 → 웹 emutest2 친구 목록에 "n/n일" 이 뜨는가
- [ ] 앱에서 공유 스위치(체중 변화) 켜기 → 나갔다 들어와도 켜져 있는가 → 웹
      emutest2 쪽에 체중 변화가 보이는가
- [ ] 비행기 모드 → 친구 탭에 목록이 남고 안내가 뜨는가 → 친구 상세 → 스위치
      → "나중에 보냅니다" → 비행기 모드 끄면 보내지는가(웹 쪽에서 확인)
- [ ] 웹 emutest2 가 운동 체크 → 앱 친구 목록 줄에 "이번 주 1/1일 운동" 과
      "운동 n일째 · 식단 n일째" → 「소식」에 뜨는가 (첫 관찰은 소식이 아닐 수
      있음 — 두 번째 체크부터)
- [ ] 앱 친구 상세: 스트릭 타일 · 오늘 식단 · 이번 주 운동 7칸이 보이는가
      (웹 emutest2 가 식단을 적으면 앱에 탄단지 막대가 뜨는가 — 웹은 diet
      스위치를 켜야 함: 웹 친구 상세의 공유 목록에 「오늘 식단 (탄단지)」)

끝나면 "7 끝" 이라고 적어 주세요. 그러면 릴리스를 끊습니다.
(측정 상세에 시각이 안 보인다는 것은 봤습니다 — 다음 판에.)


## 8. 마지막 확인 — 식단 탭(934d490) + 세 가지 고침(다음 커밋)

7번 보고 고맙습니다. 눈에 띈 것 셋 다 고쳤습니다: 토스트 별표 · 친구 상세
공유 설정 오프라인 캐시("n개 켜짐 · 서버에 못 닿아 마지막으로 본 설정") ·
친구 탭 첫 로딩(캐시 먼저 보여 주고, 요청은 한꺼번에).

식단 탭이 통째로 새로 왔습니다(위 5번 목록의 「식단 탭」 항목). 그것만
한 번 눌러 봐 주세요 — 특히:
- [ ] 「오늘」 카드 숫자가 맞는가(단백질 남음 = 목표 − 먹은 것, 칼로리 범위 90~110%)
- [ ] 「뭘 먹을까」는 요술지팡이 아이콘의 작은 버튼 → 누르면 시트가 뜨고 → 「기록에 담기」 → 시트가 닫히며 그 끼니 카드에 들어가고 「오늘」이 줄어드는가
- [ ] 끼니 카드 ✕ 로 항목 하나만 지워지는가 / 「지난번과 같이」 / 「어제 것 그대로」
- [ ] 「달성률」 주간 막대·월간 달력이 그려지는가
- [ ] 음식 검색: 분류 칩 · ☆ 즐겨찾기(다시 들어오면 위에 뜨는가) · 직접 입력
- [ ] 비행기 모드에서 친구 상세를 열면 켜진 개수가 맞게 보이는가

- [ ] **(추가) 간식 단백질 알림**: 새 빌드 첫 실행에 알림 권한을 묻는가(안드로이드
      13+). 목표가 있고 단백질이 15g 넘게 남은 채로 앱을 켜면 알림이 예약되는가 —
      `adb shell dumpsys alarm | grep -i mybody` 또는 에뮬레이터 시계를 15:29 로
      맞추고 기다리기. 문구: "단백질 n g 남았어요. 간식으로 … 어때요?".
      설정 → 「간식 단백질 알림」 끄면 예약이 사라지는가.
- [ ] **(추가) 식단 탭 위**: 「오늘 | 달성률」 고르기가 맨 위에 있고, 달성률을 고르면
      그 자리에 주간/월간이 뜨는가(맨 아래 버튼은 없어짐). 「오늘」 카드 아래에
      「주로 먹는 것」 칩(자주 먹은 순)이 끼니 카드보다 위에 있는가.
- [ ] **(추가) 친구 상세 식단 카드**: "1800 / 2445 kcal · 74%" 처럼 먹어야 하는 것 중
      얼마나 먹었는지 퍼센트가 보이는가(막대마다, 그리고 한 줄 요약).
- [ ] **(추가) 설정**: 「서버」 카드가 없고 「계정」 카드(계정 관리 · 로그아웃)만 있는가. 맨 아래 작은 글씨 「서버 주소 바꾸기 · desktop-…」이 있는가. 로그아웃하면 설정이 닫히고 로그인 화면이 뜨는가.
- [ ] **(추가) 뭘 먹을까**가 남은 탄수·지방까지 맞추는가 — 탄수를 거의 다 먹은
      날엔 밥·면보다 단백질 위주가 위로 오는가.

끝나면 "8 끝" — 그러면 릴리스를 끊습니다. 버그가 있으면 그것만 적어 주세요.


## 9. 기록 동기화(사용자 요청) — 서버 다시 띄우고 확인

서버(db.js 동의 판 2026-09-22)와 웹(sync.js)이 바뀌었습니다 — **`node tools/launch.js`
다시.** 가입 시험은 새 동의 판을 보내야 합니다(앱·웹은 이미 맞춤).

- [ ] 앱(emutest1)에서 측정·식단이 있는 상태로 **앱 데이터 삭제**(설정 → 앱 → 저장공간
      → 데이터 삭제) → 다시 켜서 로그인 → 측정·목표·계획·식단이 **그대로 돌아오는가**
      (사진만 없음). 로그인 직후 홈이 비어 보였다가 몇 초 안에 채워져도 됨.
- [ ] 웹(emutest2)에서 뭔가 저장(운동 체크 등) → 서버 DB `records` 에
      kind='state' 행이 생기는가 → 앱에서 emutest2 로 로그인(데이터 삭제 후) →
      웹 기록이 따라오는가.
- [ ] 두 기기 충돌: 앱에서 측정 추가 → 웹은 옛것 → 웹에서 저장 → 앱을 다시 켜면
      **마지막에 바꾼 쪽(웹)**이 이기는가(앱 측정이 사라짐 — 설계대로. 같은 계정을
      두 기기에서 동시에 쓰는 사람은 없다고 봄).
- [ ] 설정에 「백업」 카드가 없고 맨 아래 「내 기록 내보내기 · 가져오기」 작은 글씨만
      있는가(서버 주소 바꾸기는 없어짐 — 서버를 옮기면 앱 업데이트).
- [ ] **음식 추가 화면(새 모양)**: 둥근 검색창 → 끼니 고르기(아침/점심/저녁/간식) →
      분류 칩 한 줄 → 목록 줄마다 왼쪽 이름·성분, 오른쪽 kcal 큼직. 고르면 아래에
      담은 것 칩이 쌓이고 「저녁에 2개 저장 · 600kcal · 단백질 40g」 버튼. 양 고르는
      시트에 배수마다 kcal 이 같이 보이고 ☆ 즐겨찾기. 온보딩 동의문이 "이 기기와 내 계정에 저장" 으로 읽히는가.

끝나면 "9 끝". 그러면 릴리스.


## 10. 릴리스 끝 — 고맙습니다

v0.2.0 이 나갔습니다: https://github.com/iacobuschoi/Mybody/releases/tag/v0.2.0
마지막으로 하나만 — 에뮬레이터에 **그 릴리스 APK**(디버그 빌드 말고, 주인
열쇠로 서명된 것)를 깔아 켜지는지, 로그인·동기화가 되는지 한 번만 봐 주세요.
되면 "10 끝". 그 뒤로는 우편함을 안 봐도 됩니다. 시험 계정은 지워도 됩니다.


## 11. 운동 독촉 + 결과지 그래프 판독 — 서버 다시 띄우고 확인

서버가 바뀌었습니다(db.js 에 `pokes` 표, ocr.js 가 결과지 맨 아래 「신체변화」
그래프도 읽음) — **`node tools/launch.js` 다시.** 앱은 최신 커밋으로 새로 빌드.

- [ ] **독촉 보내기**: emutest1 앱 → 친구 탭 → emutest2 줄(오늘 체크인 없음)에
      종 모양 버튼(툴팁 「운동 독촉」) → 누르면 토스트 "emutest2님에게 운동 독촉을
      보냈습니다 💪", 버튼이 「오늘 보냄」으로 회색. 친구 상세에도 「운동 독촉하기」
      버튼. 오늘 운동 체크한 친구에게는 버튼이 안 보임.
- [ ] **독촉 받기**: emutest2 로 로그인한 앱을 켜면(또는 로그인하면) 알림
      「emutest1님이 운동하라고 콕 찔렀어요 / 오늘 운동 어때요? 💪」 + 친구 탭 맨 위
      띠. 띠의 ✕ 로 닫힘. 앱을 다시 켜도 같은 독촉이 두 번 안 옴. 웹 emutest2 에
      푸시 구독이 있으면 웹 푸시도 옴.
- [ ] **하루 한 번**: emutest1 이 같은 친구에게 다시 누르면(앱 재시작 후에도)
      "오늘은 이미 보냈습니다 — 하루 한 번만".
- [ ] **그래프 판독(앱)**: 실제 인바디 결과지 사진(주인 것 — 맨 아래 신체변화
      그래프에 3열: 26.06.30 / 26.08.31 / 26.09.19)을 「인바디 올리기」→「사진에서
      읽기」 → 안내문 끝에 "아래 그래프의 지난 측정 2개도 읽었습니다" → 「다음 —
      검산하기」 → 검수 화면 맨 아래 「지난 측정 2개도 같이」 카드(날짜 · 체중 ·
      골격근 · 체지방률, 전부 체크) → 「저장하기」 → 토스트 "저장했습니다 · 지난
      측정 2개 추가" → 추이 탭 점이 3개, 측정 기록에 6/30 · 8/31 이 있음.
      같은 결과지를 다시 넣으면 카드가 안 뜸(이미 있는 날). 체크 하나를 풀고
      저장하면 그것만 빠짐.
- [ ] **그래프 판독(웹)**: 같은 사진을 웹 P03 에서 판독 → P04 「측정 정보」 카드
      안에 체크박스 「아래 그래프의 지난 측정 2개도 같이 저장」(날짜 힌트) → 저장 →
      토스트 "측정이 저장되었습니다 · 지난 측정 2개도 추가" → 기록 화면 출처
      「그래프 복원」.
- [ ] 서버 화면 [ocr] 줄에 오류 없음. 결과지에 그래프가 없으면(다른 양식) 카드가
      안 뜨고 나머지는 전과 같음.

끝나면 "11 끝". 사진은 주인에게 받으세요 — 이 문서에는 안 넣습니다.


## 12. 같은 결과지 두 번 저장 → 두 줄 안 생김 (11번 보고에서 짚은 것) · 판 0.2.2

- [ ] 앱: 같은 결과지를 다시 판독 → 검수 화면 맨 위 초록 「이미 있는 기록과 같습니다.」
      → 저장 → 토스트 "이미 있던 기록을 갱신했습니다" → 측정 기록에 9/19 가 한 줄.
- [ ] 앱: 같은 사진이되 체중을 고쳐(예: 88.0) 저장 → 노란 「같은 시각의 측정이 이미
      있습니다.」 + 「이미 있는 기록을 덮어쓰기」 스위치(기본 꺼짐) → 안 켜고 저장하면
      두 줄, 켜고 저장하면 한 줄(88.0).
- [ ] 웹: 같은 사진 다시 판독 → 저장 → 토스트 "이미 있던 기록을 갱신했습니다", P10 한 줄.
- [ ] v0.2.2 릴리스 APK(x86_64 조각 없음)를 에뮬레이터에 그냥 `adb install` 로 깔아 켜지는가.

끝나면 "12 끝". 그 뒤로는 우편함을 안 봐도 됩니다 — 고맙습니다.


## 13. 플레이 스토어 준비 — 노트북 폴더에 정리 (주인이 출근해서 바로 올리게)

저장소 `play/` 에 글·그림·스크립트를 다 넣어 뒀습니다. 노트북에서 할 일은 **비밀이 드는 것과
실제 기기가 드는 것**뿐입니다. `git pull` 뒤 서버(`node tools/launch.js`)를 켜고:

1. **GitHub Pages 켜기** — `gh api -X POST repos/iacobuschoi/Mybody/pages -f "source[branch]=claude/body-management-app-prototype-m4mv4k" -f "source[path]=/docs"`
   (이미 켜져 있으면 409 — 그럼 됨). 몇 분 뒤 `curl -sI https://iacobuschoi.github.io/Mybody/privacy.html`
   과 `…/delete-account.html` 이 200 인지. 이메일 자리표시자는 **주인이 채움** — 건드리지 말 것.
2. **바탕화면에 `Mybody-플레이` 폴더** 만들고 저장소 `play/` 전부 복사(README.md · 등록정보.md ·
   앱-콘텐츠.md · pepk.cmd · 그림/ · make-shots.py).
3. **AAB** — `gh release download v0.2.3 -p "*.aab" -D <그 폴더>` (릴리스가 아직이면
   `gh run list -w release.yml` 로 기다림). 받은 뒤 `jarsigner -verify -verbose -certs <aab> | findstr SHA256`
   (또는 `keytool -printcert -jarfile`)로 지문이 `06d945a3…83de11` 인지 확인.
4. **심사용 계정** — 앱이나 웹에서 가입 코드로 `playreview` 가입(건강정보 동의 체크, 온보딩까지) →
   `node tools/reset-password.js playreview` 로 비밀번호 정함 → 폴더에 `계정.txt` 로 저장
   (형식은 `play/계정.txt.예시`). emutest1 과 친구 맺어 두면 심사자가 친구 기능도 볼 수 있음.
   **저장소에는 절대 넣지 말 것** (.gitignore 에 있음).
5. **스크린샷** — 이미 지금 화면으로 새로 만들어 뒀습니다(`play/그림/스크린샷/` 9장, 앱 위젯을
   실제 글꼴로 그린 것 — `cd app && flutter test test_shots/` 로 다시 뽑을 수 있음). 에뮬레이터로
   다시 찍을 필요 없음. 폴더에 복사만.
6. **열쇠 확인** — `keytool -list -v -keystore %USERPROFILE%\mybody-signing-key\mybody.jks -alias mybody`
   의 SHA256 이 `06d945a3…83de11` 인지. `pepk.cmd` 는 pepk.jar 없이 못 돌리니 파일 검사만
   (더블클릭하면 "pepk.jar 가 없습니다" 로 멈추는지).
7. 폴더의 README.md 를 열어 순서대로 되는지 훑고, 안 맞는 곳이 있으면 보고에 적기.

끝나면 "13 끝" + 폴더 경로 + 스크린샷 새로 찍었는지. 우편함은 그 뒤로 안 봐도 됩니다.

## 14. 오늘 바뀐 것 확인 — 서버 다시 띄우고 에뮬레이터·웹 (2026-09-23 오후)

13번 1(GitHub Pages)은 주인이 이미 켰습니다(Source=GitHub Actions, 두 주소 200). 건너뛰세요.
`git pull` → `node tools/launch.js` (release/ 를 새로 만들어야 웹에 새 음식표가 나갑니다) → 그리고:

1. **웹(localhost:8080)** — 식단 → 음식 추가: `김치찌게` → 「'김치찌게' 와 비슷한 이름」 에 김치찌개가
   맨 위, `ㄷㄱㅅㅅ` → 닭가슴살(초성), `ekfrktmatkf` → 닭가슴살(한/영 자판), `아이스아메리카노` →
   아메리카노, `물냉면` → 냉면(물), `zzzz` → 「찾는 음식이 없습니다」. 검색어가 바뀌지 않는지.
   `초밥`·`프로틴바`·`단백질바` 가 바로 나오는지(음식표 432개).
2. **에뮬레이터** — 최신 CI 빌드(`gh run list -w apk.yml -L 1` → `gh run download <id> -n mybody-<N>-apk`)를
   `adb install -r`. 확인: 홈에 「플랜」 카드(오늘/이번주/한달) **없음** · 식단 탭 위 「오늘/달성률」 토글
   **없음** · 플랜 탭 위 **「플랜 / 달성률」** — 달성률에 운동(계획한 날 중 지킨 날)과 식단이 주간/월간으로 ·
   홈 → 이번 주 체크인: 「얼마나 지켰나」 카드 **없고** 「지난 7일 실행」 운동·식단 숫자만 · 음식 추가
   앱바 오른쪽 「직접 입력」 · 1번과 같은 검색어가 앱에서도 같게.
3. 식단 → 「뭘 먹을까」 → 간식 탭에 프로틴바·단백질 음료가 후보로 나오는지(남은 단백질 20g 이상일 때).

끝나면 "14 끝" + 안 맞는 것. 13번이 아직이면 13 먼저.

## 15. 서버를 오라클 VM 으로 옮기기 (주인이 VM 을 만든 뒤)

`docs/ORACLE.md` 3~5단계를 합니다. 주인에게서 받을 것: **VM 공개 IP** 와 **SSH 개인 키 파일 위치**.
1·2단계(오라클 계정·VM 만들기)는 주인이 브라우저로 합니다.

1. SSH 로 들어가 `oracle-setup.sh` 실행 → 출력 확인.
2. **주인에게 "지금 서버 멈춥니다(5분)" 라고 말한 뒤** 노트북 서버 끄기 → `node tools/backup.js --out=%USERPROFILE%\mybody-move`
   → `config.json` 과 최신 백업을 scp → VM 에서 복원(계정·친구 수가 노트북 `--list` 와 같은지) → `oracle-setup.sh` 다시.
3. 주인에게 Tailscale 관리 화면에서 노트북 이름을 `laptop` 으로 바꾸게 하고, VM 에서
   `sudo tailscale up --hostname=desktop-il9c3if` (링크를 주인에게 전달) → `sudo tailscale funnel --bg 8080`.
4. 확인: 노트북에서 `curl https://desktop-il9c3if.tail0a8f8f.ts.net/health`, 에뮬레이터 앱으로 로그인·친구 목록.
5. 노트북 자동 시작(`tools/autostart.js` 로 걸어 둔 것이 있으면) 끄기 — 두 곳에서 서버가 돌면 기록이 갈라집니다.

**config.json · 개인 키 · 백업 파일은 절대 커밋하지 말 것.** 끝나면 "15 끝" + VM 셰이프(A1/E2) + 복원한 계정 수.

## 16. 0.2.6 준비 — 서버 먼저 다시 띄우기 (0.2.6 을 내기 **전에**)

0.2.6 은 동의 판 `2026-09-23` 으로 가입합니다. **옛 서버는 이 판을 거절합니다** — 서버를 먼저
새로 띄워야 0.2.6 으로 가입이 됩니다. 새 서버는 옛 판(0.2.5 이하 앱 · 심사 중인 빌드)도 계속 받습니다.
`git pull` → 서버 창 Ctrl+C → `node tools/launch.js` → 확인:

1. `node tools/test-consent.js` → 19 통과.
2. 에뮬레이터에 최신 CI 빌드 설치(14번 2와 같은 방법) → **이미 가입한 계정**으로 로그인하면
   「동의 문구가 바뀌었습니다」 화면이 한 번 뜨는지 → 동의 → 앱으로 들어가는지 → 앱을 껐다 켜면 다시 안 뜨는지.
   (「동의하지 않고 로그인 없이 쓰기」 는 로그아웃 + 로그인 없이 쓰기로 가는지 — 기기 기록은 남아야 함.)
3. 설정 → 「이 기기에서 전부 지우기」 → 로그인 화면으로 가는지 → 같은 계정으로 다시 로그인하면 기록이 돌아오는지.
4. 홈에서 계획과 어긋날 때 뜨는 「목표·기간 다시 정하기」 가 목표 화면으로 가는지 (어긋난 상태가 없으면 건너뜀).
5. 주간 체크인: 첫 체크인은 「기준 잡음」(칼로리 제안 없음) → 저장하면 홈이 「이번 주 체크인 완료 · Xkg」,
   추이 탭 체중 그래프에 점선, 체크인 화면 아래 「지난 체크인」. (두 번 연속 벗어나야 「저장하고 제안 적용」 이 나옵니다 —
   에뮬레이터 날짜로는 재현이 어려우니 안 나오면 건너뜀.)

끝나면 "16 끝" + 안 맞는 것. 오라클로 옮긴 뒤라면 VM 에서 `bash ~/mybody/tools/oracle-setup.sh update`.

## 17. v0.2.7 스토어에 올리기 — 16 은 끝났습니다 (2026-09-24 21:01 새 서버)

사용자가 **0.2.6 대신 0.2.7 을** 두 스토어에 올리기로 했습니다.
**0.2.6(아이폰 빌드 256 · `v0.2.6` AAB)은 올리지 않습니다.** 0.2.6 은 아이폰에서 알림이 전부 꺼져 있던
빌드입니다(0.2.7 에서 고쳤고, 주인 아이폰에서 된다고 확인). 0.2.6 을 이미 올렸거나 심사에 냈다면
0.2.7 로 바꿔 주세요.

빌드는 클라우드가 만들었습니다:
- 안드로이드: 태그 `v0.2.7` → 릴리스 워크플로 → Releases `mybody-v0.2.7-playstore.aab` · `mybody-v0.2.7.apk`
- 아이폰: 「아이폰 TestFlight」 워크플로(판 번호 v0.2.7) → App Store Connect 에 **0.2.7** 빌드.
  0.2.7 빌드가 둘 있습니다 — **259 는 범례 수정 전**이니 **262** 를 고릅니다.
- 확인한 것: 안드로이드 versionName 0.2.7 · versionCode 262, APK · AAB 둘 다 서명 SHA-256 `06d945a3…83de11`.
  아이폰 0.2.7 (262) 업로드 성공.

0.2.7 에 새로 들어간 것: 끼니 기록 알림(10 · 13 · 19시), 아이폰 알림 고침, 추이 그래프 범례(체크인 체중 점선).

순서:
1. 16 의 남은 둘(옛 판 계정 · 다시 로그인)은 주인이 로그인할 때 같이 봅니다 — 17 을 막지 않습니다.
2. **구글 플레이** — Releases v0.2.7 의 `…-playstore.aab` 받기 →
   `keytool -printcert -jarfile mybody-v0.2.7-playstore.aab` 로 SHA-256 `06d945a3…83de11` 확인 →
   Play Console → 테스트 및 출시 → 테스트 → **내부 테스트** → 새 버전 만들기 → AAB 올리기 →
   출시 노트는 `play/등록정보.md` 「출시 노트 → 0.2.7」 → 저장 → 검토 → 출시.
   v0.2.4 가 아직 검토 중이면 새 버전이 그걸 대신합니다(versionCode 가 더 큼).
   같은 때에 **스토어 등록정보의 자세한 설명**도 `play/등록정보.md` 대로 바꿉니다
   (「로그인 없이도 쓸 수 있습니다」 는 0.2.7 부터 맞는 말 — 파일 안 주석 참고).
3. **애플** — App Store Connect → TestFlight 에 0.2.7 (262) 가 「처리 완료」 인지 →
   앱 스토어 탭의 준비 중인 버전(지금 0.2.5 · 빌드 242)에서 **버전 번호를 0.2.7 로** 바꾸고 →
   빌드 섹션에서 242 를 빼고 **262** 를 선택 → 저장 → 심사 정보(심사용 계정 · 연락처) 확인 → **심사에 제출**.
   (빌드의 버전 문자열이 0.2.7 이라 버전 번호를 안 바꾸면 빌드 목록에 안 나옵니다.
   수출 규정은 Info.plist 에 「비면제 암호화 없음」 이 들어 있어 따로 묻지 않습니다.)
4. 심사 기간에는 노트북 서버와 터널을 켜 둡니다(심사용 로그인 · 판독 · 계정 삭제가 서버).

끝나면 "17 끝" + 올린 빌드 번호(애플) · versionCode(구글) · 안 맞는 것.

## 18. 서버를 맥미니로 옮기기 — 노트북 쪽 (맥 준비가 끝나고 주인이 "18 해" 라고 할 때)

주인이 중고 맥미니(M1 · 16GB)를 사서 **서버를 노트북에서 맥으로 옮깁니다.** 전체 순서는
`docs/MAC.md` 7절이고, 여기는 노트북이 할 몫입니다. **이 절이 15(오라클)를 대신합니다** — 오라클은
주인이 나중에 다시 고를 때만 합니다.

먼저 확인할 것:
- **16 이 끝났는지.** 0.2.6 이 이미 나갔거나 곧 나가면 16 부터 합니다. 맥은 최신 코드를 받으므로,
  옮긴 뒤에는 16 이 저절로 충족됩니다.
- 주인에게 맥 준비(`docs/MAC.md` 7-1)가 끝났는지 묻습니다. 필요한 것은 세 가지입니다.
  - 맥의 Tailscale 이름이 `mybody-mac` 입니다.
  - 맥의 원격 로그인이 켜져 있습니다.
  - 맥 계정 이름(아래 `OWNER`)을 받습니다.

순서:
1. 주인에게 **"지금 서버 멈춥니다(10분)"** 라고 말한 뒤, 서버 창에서 Ctrl+C →
   `tailscale funnel status` 가 비었는지 봅니다. 남아 있으면 `tailscale funnel reset`.
2. 서버를 끈 **뒤에** 마지막 백업을 합니다(PowerShell):
   ```powershell
   node tools/backup.js --out="$HOME\mybody-move"
   node tools/backup.js --out="$HOME\mybody-move" --list
   ```
   계정 수 · 친구 수를 적어 둡니다. 맥에서 되돌린 뒤 이 숫자와 맞춰 봅니다.
3. 맥으로 보냅니다:
   ```powershell
   scp "$HOME\.mybody\config.json" OWNER@mybody-mac:config.json
   scp "$HOME\mybody-move\mybody-<날짜>.db" OWNER@mybody-mac:last.db
   ```
   - `mybody-mac` 이 안 찾아지면 `tailscale status` 에 나온 맥의 100.x 주소를 씁니다.
   - **git · 메신저 · 클라우드 드라이브로는 보내지 않습니다.** 건강 정보와 비밀 키가 들어 있습니다.
4. 맥 쪽(`docs/MAC.md` 7-2 의 4~9)은 맥 세션이나 주인이 합니다. 노트북은 기다리다가, 주인이 Tailscale
   관리 화면에서 **이 노트북 이름을 `laptop` 으로 바꿨는지** `tailscale status` 로 확인합니다.
5. 노트북 자동 시작이 없는지 한 번 더 봅니다(작업 스케줄러 · `mybody-autostart.cmd` · 시작 프로그램).
   지난 보고에서는 없었습니다. 있으면 끕니다 — 두 곳에서 서버가 돌면 기록이 갈라집니다.
6. **이 노트북에서는 더는 `launch.js` 를 돌리지 않습니다.**
   - `$HOME\mybody-move` 와 노트북의 `server\mybody.db` 는 **2주 동안 되돌리기용으로 두고**,
     그 뒤에 지웁니다. 건강 정보가 든 사본이 노트북에 남지 않게 하려는 것입니다.
   - 2주 안에 맥이 이상하면 `docs/MAC.md` 7-2 「되돌리기」를 따릅니다.

끝나면 "18 끝" + 백업의 계정 · 친구 수 + 노트북의 새 Tailscale 이름을 보고합니다.
**이 뒤로 노트북 세션은 LOCAL-TASKS 를 받지 않고, 맥 세션이 이어받는 것을 기본으로 합니다**
(`docs/MAC.md` 6절). 스토어 콘솔 올리기(17 같은 일)는 맥이든 노트북이든 브라우저로 됩니다.

## 19. 심사용 계정에 샘플 기록 넣기 · 심사 기간 서버 지키기 (0.2.7 심사 중)

17 보고의 「안 맞는 것」 1 · 2 에 대한 답입니다. 주인이 **노트북에 맡긴다** 고 했습니다.
`playreview` 에 기록이 없어 심사자가 빈 앱에서 시작하고, 옛 동의 판이라 「동의 문구가 바뀌었습니다」 가
한 번 뜹니다. **앱으로 직접 넣습니다** — 서버 DB 에 손으로 넣으면 앱의 동기화 형식과 어긋날 수 있습니다.
**비밀번호 입력만 주인 몫**입니다(`play/계정.txt`). 비밀번호는 바꾸지 않습니다 — 애플 · 구글에
낸 값입니다(`tools/reset-password.js` 금지).

1. 에뮬레이터에 **0.2.7** 을 깝니다: Releases v0.2.7 의 `mybody-v0.2.7.apk`(심사자가 쓰는 판).
   에뮬레이터의 기존 앱 기록은 「전부 지우기」 로 비우고 로그인 화면에서 시작합니다.
2. **주인에게** `playreview` 로그인을 부탁합니다(아이디 · 비밀번호 입력까지). 그 뒤는 노트북이 합니다.
3. 「동의 문구가 바뀌었습니다」 → **동의**. 이러면 심사자에게는 다시 안 뜹니다.
4. 온보딩은 **샘플 값**으로 합니다 — 주인의 실제 몸 숫자를 넣지 않습니다.
   - 남 · 1996년생 · 175cm (온보딩이 묻는 나머지는 기본값)
   - 인바디 두 번(추이가 선으로 보이게). 인바디 올리기 → 숫자 입력 → 측정 날짜를 바꿔서:
     - **4주 전** 날짜: 체중 **76.0** · 골격근 **32.4** · 체지방 **17.2** kg
     - **오늘**: 체중 **75.1** · 골격근 **32.6** · 체지방 **16.3** kg
     - 검산 경고가 나오면 그 문구를 적어 보고하고, 값은 그대로 둡니다.
   - 목표: 체지방 **13kg** 쯤으로 줄이기 → 기간 고르기에서 **추천 강도** → 계획 만들기.
   - 식단에 오늘 끼니 하나(예: 점심 · 제육덮밥) — 식단 화면이 비어 있지 않게.
5. 화면 확인: 홈 · 플랜 · 추이(체중 · 골격근 · 체지방 선이 둘째 점까지) · 식단이 채워져 보이는지.
6. **로그아웃**(설정 → 로그아웃). 「전부 지우기」 가 아니어도 됩니다. 기록은 계정에 남습니다.
7. 서버에서 `playreview` 의 `records` 가 이제 비어 있지 않은지 확인합니다(17 에서 본 방법 그대로).
8. **친구 기능:** `playreview` 와 두 번째 계정(`emutest1`)이 이미 친구인지 봅니다(서버 `friendships`).
   아니면 주인에게 알리기만 합니다 — 심사자가 메모의 초대 코드 안내대로 맺을 수 있으니 막는 일은 아닙니다.

**심사 기간(최대 48시간) 서버 지키기** — 자동 시작이 아직 없어서, 노트북이 꺼지거나 잠들면 심사자가 로그인을
못 하고 **거절될 수 있습니다.**
- 윈도우 설정 → 시스템 → 전원: 화면 · 절전 **「안 함」**(전원 연결 시), 덮개를 닫아도 **아무 것도 안 함**.
- Windows 업데이트 → **1주 일시 중지** — 심사 도중 자동 재시작을 막습니다.
- `launch.js` 창은 닫지 않습니다(최소화는 괜찮음). 끝나면 원래 설정으로 돌려도 됩니다.

끝나면 "19 끝" + 넣은 값 · records 수 · 친구 여부 · 전원 설정.
